# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repository is

Design package for **VideoForge** (repo directory: `media-atlas`) — a self-hosted, trend-driven, multilingual short-video re-creation system. First target: Douyin + TikTok, 中文 ↔ English, AI/tech content, macOS pilot. Full loop: trend discovery → video acquisition → AI analysis (`VideoBlueprint`) → two re-creation modes → localization → review → publishing → performance feedback.

**Current state: P0 complete (exit gate passed); P1 in progress — VF-101 Trend Domain, VF-102/103 Douyin & TikTok Discovery, VF-104 Trend UI, VF-105 URL Resolver + yt-dlp Adapter, VF-106 f2 Adapter, VF-107 Fingerprint + Dedup, and the P1 Exit acceptance gate all done — P1 closed. P2 closed (VF-201…VF-205 + P2 Exit gate). P3 closed — VF-301..VF-310 all done + P3 Exit acceptance gate passed. P4 localization in progress — VF-401 Canonical Localization + VF-402 Subtitle Engine + VF-403 On-screen Text Localization + VF-404 TTS Providers + VF-405 Duration Fit + Audio Mix + VF-406 LipSync Provider + VF-407 Localization Review done (VoiceProfile 授权红线 + CLONED sample/consent 合同层硬拦 + TTSProvider Protocol + TTSRouter 终局/回退语义 + Voice Profile 双保险; VF-405 五步时长拟合"禁止极端压速"红线 + §9 Ducking/True Peak Gate 护栏 + opus 三轮 CONFIRMED; VF-406 §10 口型**非阻塞**红线 + §10.1 资格判断 + §10.2 回退阶梯 + opus 两轮 CONFIRMED; VF-407 §12 语言 QA 聚合 + §13 局部重跑作用域 + 数字一致 BLOCKER + opus 6/6 CONFIRMED). P4 本地化 CLOSED——P4 Exit 验收关口通过（20 中英样本 40 句：数字/专名/否定一致 100% + 字幕阅读速度全过 + 口型失败自动降级不阻塞 + 改单句只重跑该句作用域；opus 6/6 CONFIRMED 非空跑）。P5 审核与发布 CLOSED — VF-501 Review Policy + Queue + VF-502 Platform Preflight + VF-503 TikTok Official Publish（**Fake-first，无 live network**）done（VF-501 版本签名/修改失效 + ALWAYS/NEW_TEMPLATE_ONLY/AUTO 策略 + FATAL 不可覆盖 + Trusted Template NEW→TRUSTED 状态机 + opus 安全向 5/5 CONFIRMED 无 bypass；VF-502 发布前检查 媒体/元数据/授权/账号 + §3 连接器能力上报 + 方法优先级阶梯 + opus 5/5 CONFIRMED，连接器只上报能力不触真实平台；VF-503 幂等发布状态机 + §5 重试不重复发布红线 + Fake/Unconfigured executor 不触真实平台 + opus 5/5 CONFIRMED grep-clean 无网络；VF-504 Douyin Official Publish（Fake-first）抖音 executor + §3.2 OpenAPI 错误映射（授权/风控 human_required、限流 retryable、优先级从严）+ 执行器 DRY 基类（TikTok/抖音共用幂等核心）+ opus 5/5 CONFIRMED 无网络无 VF-503 回归；VF-505 Browser Publish Adapter（Fake-first）§6 白名单 host 锚定（WHATWG 一致，反斜杠/fragment/%编码绕过全防）+ 挑战暂停不绕过（登录/验证码/设备/内容警告 → 人工）+ 选择器优先级 + Canary + opus 两轮 CONFIRMED（round1 揭反斜杠绕过 → 修 WHATWG 一致 + fail-closed LDH）；VF-506 Android Device Adapter（Fake-first）§7.2 设备就绪门（**前台账号红线 never 发错号** + 电量/存储/网络/解锁/App 版本）+ 单设备单 Job + `PlatformAccount` 无 secret（§8 只存不透明 handle）+ opus 两轮 CONFIRMED（round1 揭 credential_ref 前缀检查漏嵌入 JWT → 修子串扫描）。VF-507 Calendar + Reconciliation（发布窗口日程 next_publish_time 支持跨零点 + 副本幂等键 copy_index≥1 才派生新键防重复发 + 手工完成 mark_manually_completed + 修 VF-503 reconcile-from-WAITING_FOR_HUMAN 潜在迁移 bug + opus 5/5 CONFIRMED 无双发回归）。P5 审核与发布 CLOSED——P5 Exit 验收关口通过（VF-501..507 Fake-first 端到端发布 + 网络超时重试不重复发 + 挑战必停给人工 + 版本改动审批失效 + 前台账号确认，opus 5/5 CONFIRMED 非空跑）。P5 骨架含 TikTok/抖音/浏览器/Android 四适配器 + 幂等状态机 + 审核策略 + 预检 + 日程/对账。⚠️ **真实平台/浏览器/真机 live 集成（官方 API、Playwright、Appium/ADB 驱动真实账号）仍是 stop-condition**（需真实账号 + 应用审核 + 凭据）.** P6 效果反馈 in progress — VF-601 Metrics Connectors + VF-602 Performance Dashboard + VF-603 Learning Signals done（VF-601 §10 平台指标快照 + 限流 + **缺失指标保持 null 不填 0** 红线 + Fake-first 无网络 + opus 5/5 CONFIRMED 无 REFUTED；VF-602 §11 账号内相对指标 + 模板/Hook/时长/语言/发布时间分组 + **样本不足不排序** + null≠0 + opus 5/5 CONFIRMED 无 REFUTED；VF-603 §11 过程信号↔相对表现 Spearman 秩相关 + **关联非因果** 红线 + 样本不足不下结论 + opus 5/5 CONFIRMED 无 REFUTED）。 Layers so far: VF-001 skeleton (uv + pnpm workspaces, seven-service compose stack, CI) → VF-002 contracts (`packages/contracts-py` Pydantic truth → `schemas/` JSON Schema artifacts → `packages/contracts-ts` generated types; `make schemas` regenerates; TaskEnvelope rejects plaintext-credential keys; compat fixtures in `packages/contracts-py/tests/fixtures/v<N>/` are immutable) → VF-003 persistence (`packages/persistence`: SQLAlchemy 2 tables, repositories that only speak contract objects, optimistic locking via `version` column, transactional outbox + consumer-side `try_claim_event` dedupe; Alembic migrations at `migrations/`, `make migrate` targets the dev DB). → VF-004 artifact store (`packages/media-core`: boto3 ObjectStore, staged-upload→verify→commit ArtifactStore returning contract objects, content-addressed LocalArtifactCache with hash-verified self-healing, streaming sha256, ffprobe probe via args-array subprocess; media-core never imports persistence — application layer composes them; `new_id` lives in `videoforge_contracts.ids`). → VF-005 temporal skeleton (`packages/workflows`: PipelineSkeletonWorkflow with review-wait Signal/Query and explicit RetryPolicy, zero I/O in workflow code; `apps/temporal-worker`: `make worker` runs the core-queue worker; `apps/api` `/v1/operations`: Idempotency-Key-derived workflow ids, service injected via app.state so tests use stubs). → VF-006 provider registry (`packages/provider-sdk`: descriptor.yaml→contract loader with first fakes under `connectors/`, ProviderRegistry with kill-switch/stats, CircuitBreaker closed→open→half-open with single-probe reservation — `allow_request` is a pure peek, routing reserves the probe on selection; `route()` implements the 30 §6 weight formula verbatim and returns explainable breakdown + rejection reason codes). Integration tests use testcontainers (throwaway postgres/minio; skip when docker unavailable; Ryuk disabled — this machine's Docker Desktop 500s on its socket-mount) plus Temporal time-skipping envs (SDK auto-downloads the test server; test workers set `max_cached_workflows=0` so restart tests replay from history and queries never wait on a dead worker's sticky queue). Notes: INSERT rowcount is unreliable under psycopg3 — use RETURNING; test dirs must not share module basenames (pytest top-level import collision — e.g. `provider_factories.py` not `factories.py`). → VF-007 part 1/2 (Task Lease + Edge Agent: `workers`/`worker_tasks` tables with `FOR UPDATE SKIP LOCKED` atomic claim — expired leases are reclaimable with no background sweeper; row-locked renew/complete/fail to kill stale-commit races; complete is idempotent by task_id+output_digest even across lease reassignment; `apps/api` worker routes are sync handlers over a per-call-transaction DbWorkerGateway, api now owns a DB engine via `settings.database_url`; `services/edge-agent` is `make agent` — registers with ffmpeg/ffprobe toolchain probe, heartbeats, claims, executes via provider-sdk Fakes built from `connectors/`, submits canonical-JSON sha256 digests; `logsafe` scrubs env-derived secrets from log messages AND pre-rendered exception tracebacks, and fail() reasons are scrubbed before reaching the server). → VF-007 part 2/2 (`apps/desktop`: Tauri 2 shell, `make desktop`; sidecar spawned via `process_group(0)` + pidfile so it survives UI exit — never use Tauri-managed child processes for it; `beforeDevCommand` uses `corepack pnpm`; Keychain broker behind a `CredentialStore` trait — keyring `apple-native` in prod, in-memory in tests because keyring's mock doesn't share state across Entry instances; `unseal` is deliberately NOT a Tauri command so the WebView can never read secrets; app icon required by `generate_context!` even with bundle off). Rust toolchain now installed via rustup (stable, `~/.cargo`; rustfmt+clippy added). → **P0 Exit gate** (`packages/workflows/lease_bridge.py` + `apps/temporal-worker/dispatch.py`): `PipelineViaLeaseWorkflow` (pipeline-via-lease) dispatches each stage by string-named activity `dispatch_to_worker`, whose impl enqueues a worker-task and polls the lease queue until the desktop edge-agent completes it (Temporal heartbeat while polling; enqueue idempotent by `{workflow_id}-{run_id}-{stage}` so activity retries reuse the task but a workflow reset re-executes; sync activity on a ThreadPoolExecutor; `max_wait_s=None` in prod relies on Temporal's start_to_close timeout). Acceptance proven on the dev stack: a long Temporal workflow runs Fake Source→Fake Render entirely through the lease queue executed by the desktop agent, both worker-tasks COMPLETED, status visible via API + Temporal. **All seven P0 tasks plus the P0 Exit acceptance are done; P0 is closed.** Known deferrals into P1: worker-tasks have no terminal FAILED state / attempt cap (a permanently-failing capability can spin the queue and stall a workflow up to ~50min — flagged as a follow-up task); the lease pipeline's Fake outputs are inline JSON, not VF-004 ObjectStore artifacts (wire real media in P1). → **VF-101 Trend Domain** (first P1 task): contracts `TrendItemSnapshot` (nullable metrics — null never 0), `TrendCluster` (aggregate, optimistic `version`), `HotScoreWeights` (template-versioned, ≥0 magnitudes — the negative sign of saturation/decay lives in the formula, not the weights), `TrendSubScores`, `TrendStage` enum; **`packages/domain`** (`videoforge_domain`, pure — imports only stdlib + contracts, no frameworks) holds the trend kinematics (velocity = latest-segment rate/hour, acceleration = 2nd difference, engagement-efficiency with account-size damping, decline-from-peak → decay), `hot_score` aggregation (coefficients read from the weights template, replayable to the bit), the stage classifier + `STAGE_TRANSITIONS` (forward-skips allowed since infrequent observation can jump stages, so the classifier's output is always a legal move; DECAYING→RISING re-ignite; ARCHIVED terminal), and reason-code derivation. → **VF-102 Douyin Discovery** (first real platform connector): discovery port in provider-sdk (`DiscoveryConnector` protocol, `DiscoveryRequest/Result`, `DiscoveryMode`, `DiscoveryStatus` = ok/empty/failed/unconfigured/challenge, `ConnectorErrorCode` = the 12 codes from 51 §12); `connectors/sources/douyin` (uv member `videoforge-connector-douyin`, first `connectors/` package with real code — parser/error_mapping/fetcher/connector/descriptor/fixtures/tests). Safety posture baked in per the stop conditions: **no live network code anywhere** (grep-clean), official API is an unconfigured stub returning `UNCONFIGURED`/`APP_REVIEW_REQUIRED` (needs real creds + app review), the L4 public-signal path parses only a repo-neutral shape (NOT Douyin's private API — no reverse-engineered signatures) behind recorded fixtures, live fetching is deliberately unimplemented, captcha/risk → `CHALLENGE` routed to human (never bypassed), and manual import is always available. Empty results are diagnosable: `EMPTY` (fetched, no data) vs `FAILED` vs `UNCONFIGURED` — never a silent empty board (40 §9/§10). Connector packages depend only on contracts + provider-sdk. → **VF-103 TikTok Discovery + connector kit**: the discovery machinery (parser, error mapping, fetcher stubs, and the `discover()` orchestration) is now a reusable kit in provider-sdk (`connector_errors` / `fetcher` / `neutral_parser` / `NeutralDiscoveryConnector`); each platform connector is a thin subclass with platform config (name, `platform`, collector version, source-confidence, descriptor). Douyin was refactored onto the kit (its `__init__` re-exports the kit names for a self-contained import surface); `connectors/sources/tiktok` (`videoforge-connector-tiktok`, L4, en-US/zh-CN) is thin on top. The connector owns its platform for parsing — `DiscoveryRequest.platform` is just an optional hint (default `""`), so a request never mislabels a snapshot. `map_payload_error` short-circuits to None when the payload has an `items` list (a data response never carries an error/challenge), so business field names can't false-trip challenge detection; challenge markers otherwise scan only error/message fields + top-level keys, never item data. New connector packages: **thin subclass + descriptor + fixtures + tests only** — shared logic lives in the kit. Test dirs still can't share module basenames (`fixtures_helper.py` vs `tk_fixtures.py`). → **VF-104 Trend UI** (full vertical slice — persistence + API + Vue): migration `0003` adds `trend_clusters` + `trend_item_snapshots`; `TrendClusterRepository` (list ordered by hot_score desc nulls-last with stage/vertical filters, optimistic-lock `update`, `merge` that unions members/keywords and archives the source (never deletes) keeping `MERGED_FROM`/`MERGED_INTO` provenance, `split` that moves members to a new cluster AND partitions snapshot_ids by item_id); `TrendItemSnapshotRepository` (append-only). `videoforge_domain.rescore_cluster` is a pure function (contracts in/out, no I/O, replayable) — buckets cross-item snapshots by observed_at into an aggregate series, runs the VF-101 kinematics, keeps `MERGED_`/`SPLIT_` provenance codes. `apps/api` `/v1/trend-clusters` (create/list/get/snapshots→attach+rescore/merge/split/rescore/projects→one-click Project with trend_cluster_id + outbox event) via `DbTrendGateway` on app.state. `apps/web` TrendBoard (hot/stage/reason badges, filters, empty-vs-error states) + TrendDetail (subscores, evidence, rescore/split/one-click-Project actions all sending `expected_version` for optimistic lock; 409 shows a conflict message and reloads — never swallowed). Note: `trend.py` needs `from __future__ import annotations` because its `list` method shadows builtin `list` for later `list[str]` annotations. Known deferrals: `_aggregate_series` buckets by exact `observed_at` (time-window bucketing when collectors add jitter — VF-604 calibration); merge doesn't auto-rescore the target (human clicks rescore). → **VF-105 URL Resolver + yt-dlp Adapter** (start of M02 acquisition): acquisition port in provider-sdk (`acquisition.py`: `DownloadConnector` protocol, `ResolvedSource`/`DownloadRequest`/`DownloadResult`/`AcquisitionManifest` dataclasses, `DownloadStatus` = ok/failed/unconfigured/challenge/auth_required, `AcquisitionErrorCode` = the 41 §12 download codes + operational challenge/unconfigured/unknown — a SEPARATE set from discovery's `ConnectorErrorCode`; `acquisition_input_digest` = sha256(platform|content_id|canonical_url|format) as a replayable cache key). `url_resolver.py` is pure (no network): platform is anchored to the URL **host** (not a domain substring in the path, else `evil.com/douyin.com/video/…` mislabels), short links (v.douyin.com / vm.tiktok.com) return `needs_expansion` for a pluggable `ShortLinkExpander` (Fixture/Unconfigured — live expansion needs network, deliberately off), plus share-text URL extraction and `resolve_local_file` → `platform=manual` (always available). `download_errors.py` `map_ytdlp_error` maps yt-dlp stderr signatures to §12 codes with ORDER = priority (challenge first; codec's "format is not available" before the "not available" substring; unavailable's "not found" narrowed to "video not found" so "moov atom not found" stays MEDIA_CORRUPT). `connectors/downloaders/yt-dlp` (`videoforge-connector-yt-dlp`, L4, first `connectors/downloaders/` pkg) is thin on the port: `runner.py` mirrors the ffprobe pattern (args-array subprocess; `UnconfiguredYtDlpRunner` default returns UNCONFIGURED; `SubprocessYtDlpRunner` needs an explicit binary + probes `--version`), `cookies.py` resolves an opaque `credential_handle` → short-lived cookie file (default Unconfigured → AUTH_REQUIRED, never silent-anonymous), `connector.py` `build_argv` is injection-safe (every caller value as a single `--opt=value` token, URL after `--`, never a shell string), `download` returns a replayable `AcquisitionManifest` (tool_version from runner, output sha256 = real file hash), located media is constrained to `dest_dir` (won't return an out-of-dest absolute path from a malicious info-dict), and returned `raw_metadata` is recursively scrubbed of cookie/authorization/token/http_headers keys. yt-dlp version LOCKED at 2025.01.15 (`runner.PINNED_VERSION` ↔ descriptor, test-asserted), registered in `third_party_manifest.yaml` (Unlicense but L4 for the platform-extraction use). Safety: no live network (grep-clean), captcha/risk → CHALLENGE routed to human, `--continue`/`--no-overwrites` = resumable + idempotent (rerun → same output sha256). Known limitation: yt-dlp sometimes reports a hidden captcha as an extraction failure → CONNECTOR_SCHEMA_CHANGED (still human/breaker-routed); YouTube bot-check "sign in" text classifies AUTH_REQUIRED not CHALLENGE (both human-routed). Deferred: the persisted SourceAsset aggregate + IngestWorkflow + `/v1/sources:resolve`/`import-url` endpoints (this task is resolver + adapter machinery only). Connector test dirs still can't share module basenames (`ytdlp_fixtures.py`). → **VF-106 f2 Adapter** (Douyin/TikTok download, priority + fallback vs yt-dlp): the DownloadConnector port gets a SECOND impl (f2) plus a `DownloadRouter`. `DOWNLOAD_PRIORITY` = douyin:[f2, yt_dlp], tiktok:[yt_dlp, f2], youtube:[yt_dlp] (41 §2). **Router fallback semantics are safety-critical**: OK/CHALLENGE/AUTH_REQUIRED are TERMINAL — a captcha/login wall on one downloader must NOT trigger trying another (that would bypass the risk control the platform raised → WAITING_FOR_HUMAN); only UNCONFIGURED + other FAILED codes fall through to the next provider; chain exhausted or unknown/manual platform → `manual_fallback=True` with an explainable `attempts` trail. Shared machinery promoted into provider-sdk so BOTH download connectors use ONE tested impl of the security-critical bits: `credentials.py` (`CookieResolver` moved up from yt-dlp), `download_helpers.py` (`scrub_metadata` recursive secret scrub / `sha256_file` / `locate_downloaded_media` with dest_dir constraint), and `map_ytdlp_error`→`map_download_error` (tool-neutral, + Chinese/f2 signatures, markers narrowed so benign text like "已删除"/"频繁" doesn't false-trip). `connectors/downloaders/f2` (`videoforge-connector-f2`, L4 — Apache-2.0 but platform-extraction use is L4) is thin: `runner.py` (F2Runner, Unconfigured default / Subprocess real, args-array), `connector.py` (f2 CLI `dy`/`tk` subcommand from a fixed dict, `--opt=value` single-token argv with NO positional URL, reuses the shared cookie/error/helper machinery). f2 version LOCKED at 0.0.1.7 (PINNED_VERSION ↔ descriptor, test-asserted), registered in `third_party_manifest.yaml`. yt-dlp connector refactored onto the shared helpers (its `cookies.py` deleted; behavior unchanged, tests still green). Safety unchanged from VF-105: no live network (grep-clean), captcha → CHALLENGE → human, cookies via opaque handle. Deferred: real f2 CLI output adaptation (finalized when live download is enabled). → **VF-107 Fingerprint + Dedup** (last P1 task): dedup is a PURE domain concern. `packages/domain/fingerprints.py` (pure, replayable): `text_simhash` (char 3-gram + blake2b + bit-voting, stable for zh+en), `dhash_from_gray` (difference hash from a grayscale byte matrix), `hamming`, `phash_sequence_similarity` (bidirectional nearest-neighbour hit-rate — robust to transcode/light-trim/frame-rate diffs). `packages/domain/dedup.py` `find_duplicate_groups` unions assets by layer (FILE exact sha256 / VIDEO pHash-sequence / AUDIO exact / TEXT simhash-hamming) into connected components (union-find, transitive); deterministic `group_id` from sorted members (\x00-joined); **only builds groups + similarity, NEVER deletes a source record** (41 §5); returns only ≥2-member groups. Thresholds calibrated from real data (video_frame_max_hamming=10, video_seq_min_similarity=0.8, text_max_hamming=8 — measured: single-char edit ≈4-5 bits, different script ≈31). Empty-text simhash is 0 → treated as "no text" so blank transcripts never false-group. `packages/media-core/video_fingerprint.py` (real, ffmpeg args-array): `FfmpegVideoFingerprinter` extracts frames (`fps=1/N,scale=9:8,format=gray,rawvideo`) → dHash sequence via domain's pure `dhash_from_gray`; media-core now depends on `videoforge-domain` (legal — domain is the pure lower layer; media-core→domain, never the reverse). Integration test (skip if no ffmpeg) proves acceptance §13 on REAL media: two transcodes of a testsrc clip (320x240 crf20 vs 480x360 crf32) group together (per-frame hamming 1-3, sequence sim 1.0), mandelbrot doesn't (0.0). Audio chromaprint/fpcalc not installed here → audio-fingerprint layer deferred (the dedup AUDIO layer is ready; fpcalc registered in `third_party_manifest` as L3). Known: dHash false-groups mostly-uniform (blank) frames — perceptual-hash universal limitation, surfaced to human review. Deferred: `AssetFingerprint`/`DuplicateGroup` persistence + IngestWorkflow wiring (this task is the pure engine + real video fingerprinter). → **P1 Exit acceptance gate** (`tests/acceptance/`, report at `docs/implementation/54a-p1-exit-report.md`): a runnable acceptance suite wired into pytest `testpaths` (so `make test` enforces it). Acquisition: 61 synthetic pilot links (douyin/tiktok/youtube canonical + share-text + short-links + modal_id/item_id + unsupported + local) → `resolve` + `DownloadRouter` (Unconfigured connectors, live download is the stop condition) → **0 silent failures, 100% clear disposition** (34 resolved→manual-fallback, 20 needs-expansion, 4 unresolvable-clear-error, 3 manual) — the "importable OR clear manual fallback" OR-branch. Trend: 24 clusters rescored (pure `rescore_cluster`), Top-20 hot_score 0.5272-0.5685 with **20 distinct scores** (real ranking), each with reason_codes + sub_scores (evidence) + bit-identical replay across processes. Dedup: same-content-different-transcode → one group, sources independent. **P1 is CLOSED.** Deferred into P2/later (honestly listed in the report, not papered over): live download (needs real accounts), audio chromaprint fingerprint (fpcalc not installed), interrupt-resume dedup, and `SourceAsset`/`DuplicateGroup` persistence + IngestWorkflow wiring. → **VF-201 Media Runtime** (P2 start): the foundational media-op runtime every P2 analysis builds on. `packages/media-core/job_manifest.py`: `JobManifest` (op, schema_version, input digests, tool, tool_version, config, output digests) + `activity_cache_key` = the 41 §11 formula `sha256(input_digest + provider + tool_version + normalized_config + schema_version)` — the replayable-manifest + local-re-run cache primitive; `JobManifest.cache_key` uses first input + op + tool_version + config and **deliberately excludes output hashes** (same input+config = cache hit, so "change captions → don't re-run download/scene"). `media_runtime.py` `FfmpegRuntime` (probes ffmpeg/ffprobe versions at construction → into every manifest): `make_proxy` (720p H.264, duration/timebase preserved), `extract_audio` (48kHz mono WAV for ASR), `detect_scenes` (scene-cut timestamps), `probe` — ALL ffmpeg/ffprobe as **args-array subprocess** (no shell, injection-safe: a malicious `$(...)` path is just an argv element, verified), each failure → unified `MediaRuntimeError` (detect_scenes goes through the same `_run` wrapper — a verifier-caught bug, now fixed + regression-tested). `face.py`: `FaceDetector` port + `NullFaceDetector` (configured=False, empty — honest "not wired", never fakes faces) + `FakeFaceDetector` (deterministic, for downstream dev); real face detection needs a model → deferred with ASR/OCR/VLM. Proven on real ffmpeg 8.1.2: 720p proxy preserves duration, extracted WAV independently ffprobe-verified 48000Hz mono, a testsrc→mandelbrot 6s clip yields a cut at exactly 3.0s. `FfmpegNotAvailable` promoted to `errors.py` (single definition; VF-107 fingerprinter imports it there). Note: test clips that need a hard cut must put `-t <dur>` BEFORE the second `-i` (input option) — putting it after truncates the whole output and there's no cut (a real bug hit during dev). Deferred: real face/motion model, JobManifest persistence + AnalysisWorkflow wiring. → **VF-202 ASR Providers**: the unified word-level transcript CONTRACT (the doc says 合同, so it's a real contract). `contracts-py/transcript.py`: `Transcript` / `TranscriptSegment` (per-segment `language` for mixed-language) / `TranscriptWord` (word start/end/confidence + `low_confidence`) / `TranscriptModels` (asr/vad/align/speaker versions for replay), with `model_validator`s rejecting `end_ms < start_ms`; registered in `CONTRACTS` → `make schemas` regenerated `schemas/transcript.schema.json` + contracts-ts, plus a `tests/fixtures/v1/transcript.json` compat fixture + `samples.py` sample + `test_invalid.py` negatives (incl nested span). `domain/transcript.py` `mark_low_confidence` (pure, returns a new Transcript flagging words/segments below thresholds — a segment is flagged if its OWN confidence is low OR it contains a low-confidence word) + `needs_review` + `low_confidence_spans`. `provider-sdk/asr.py` (contracts-only, NO domain dep — fine-grained marking is the caller's domain pass): `ASRProvider` port + `ASRRequest`/`ASRResult`/`ASRStatus` (OK / NEEDS_REVIEW / FAILED / UNCONFIGURED / UNSUPPORTED_LANGUAGE) / `ASRErrorCode`; `UnconfiguredASRProvider` (honest UNCONFIGURED — real engines need models → deferred); `FakeASRProvider` (replays recorded transcripts by language; hotword bias boosts matching words' confidence AND only records hotwords it actually applied; gross NEEDS_REVIEW below a review floor — a low-confidence transcript is still `ok=True`, routed to human not discarded); `ASRRouter` orders by language-filter → hotword-capable-first → local/cloud policy → name, fallback over UNCONFIGURED/FAILED, terminal on OK/NEEDS_REVIEW; `LanguageSegmenter` port + Fake/Unconfigured for mixed-language pre-split. Recorded zh/en fixtures. The Fake is honest (verifier-caught, fixed): a non-hotword provider never falsely records hotwords, and a declared-but-unrecorded language returns FAILED rather than replaying a wrong-language transcript. ASR engines (whisper.cpp/faster-whisper/WhisperX/FunASR) registered in `third_party_manifest` as L1 with model weights deferred. Deferred: real ASR engines + weights, real language-segmentation/diarization models, Transcript persistence + AnalysisWorkflow wiring. → **VF-203 OCR + TextTrack** (same contract + pure-domain + provider-sdk-port/Fake shape as ASR). `contracts-py` (`TextTrackKind` enum = CAPTION/TITLE/LOWER_THIRD/UI/SCENE_TEXT/BRAND_MARK/UNKNOWN) `texttrack.py`: `BBox` (normalized x/y/w/h + in-frame validator), `TextObservation` (per-frame OCR: frame_time/bbox/text/confidence/occluded — the raw type shared across the provider→tracker boundary, so neither side owns it), `TextTrack` (kind/voted text/span/confidence/observations trajectory/motion/style/low_confidence, end≥start), `TextTrackSet` (top-level). Registered `text-track-set` → make schemas + contracts-ts + v1 compat fixture + negatives (incl nested span + bbox-out-of-frame). `domain/texttrack.py` (pure): `iou`; `track_text_observations` greedy IoU association — **associate when IoU≥threshold AND (within gap_ms OR same text)**; the same-text clause lets a sparsely-sampled persistent watermark (frames 20s apart) stay ONE track, while a *different* caption at the same bottom spot after a gap correctly splits into two (both verifier-probed); `vote_text` multi-frame majority vote with deterministic tie-break (count→total-conf→lexicographic), confidence = mean of the winning text's frames so one misread frame doesn't drag it down; `classify_kind` heuristic (bottom-wide→CAPTION, top-large→TITLE, small-corner-persistent[duration/total>0.6]→BRAND_MARK, small-corner-short→UI, mid→SCENE_TEXT); motion STATIC/MOVING; low-confidence flag. `provider-sdk/ocr.py` (contracts-only): `OCRProvider` port + `UnconfiguredOCRProvider` (honest UNCONFIGURED) + `FakeOCRProvider` — **deep-copies observations in AND out** so a caller mutating a returned observation never pollutes the recording (a verifier-caught element-level leak — ContractModel is validate_assignment, not frozen — fixed + element-level regression test). Recorded observations fixture + an end-to-end OCR→tracking test proving voting corrects a noisy frame and the watermark classifies BRAND_MARK. OCR engines (PaddleOCR/RapidOCR) registered L1, weights deferred. Deliberate tradeoff: same-text-bridges-gaps can lengthen a repeated-identical-caption's span (accepted for watermark recognition). Deferred: real OCR engines + models, optical-flow tracking refinement, TextTrackSet persistence + AnalysisWorkflow wiring. → **VF-204 VLM Sampling** (cost-line: **never per-frame cloud calls**): `contracts-py` (`FrameSampleReason` enum) `vlm.py`: `FrameAnalysis` (frame_time/reasons≥1/caption?/labels/confidence? — caption null = selected-but-unanalyzed), `VisualAnalysis` (frames/sampling_policy/vlm_provider?/vlm_version?). Registered `visual-analysis` → schemas + TS + v1 compat + negatives. `domain/frame_sampling.py` `select_representative_frames` (pure, id/created_at passed in so no clock, replayable): unions candidate frames from scene cuts (SCENE_CUT), text-track starts (TEXT_CHANGE), speaker changes + low-confidence OCR-tracks/ASR-segments (SPEAKER_CHANGE/LOW_CONFIDENCE), periodic coverage (PERIODIC), frame 0 (KEYFRAME); merges near frames within a window (combining reasons); and **caps at max_frames by reason priority** (KEYFRAME>SCENE_CUT>LOW_CONFIDENCE>SPEAKER_CHANGE>TEXT_CHANGE>PERIODIC — periodic dropped first) so a 1-hour video with 72k scene cuts still yields ≤40 frames (verifier-probed), *structurally* preventing per-frame calls. `provider-sdk/vlm.py` (contracts-only): `VLMProvider` port whose `analyze` takes the WHOLE frame batch in ONE call (batch-by-shape) + `UnconfiguredVLMProvider` (honest UNCONFIGURED) + `FakeVLMProvider` (replays recorded captions by frame_time; `call_count` proves single-call batch; uncaptioned frames stay None — never fabricated; deep-copies IN and OUT so neither the request nor the recording is polluted). Recorded captions fixture + end-to-end selection→analysis test. VLM engines (Qwen-VL/Claude/local) registered L2, deferred; result caching via VF-201 `activity_cache_key`. Deferred: real VLM + budget guard, VisualAnalysis persistence + AnalysisWorkflow wiring. → **VF-205 Blueprint Fusion** (P2 capstone — fuses ASR/OCR/VLM into `VideoBlueprint`): `contracts-py` (enums `RhetoricalBeatKind`/`VisualBeatKind`/`ClaimSourceStatus`) `blueprint.py`: `EvidenceSpan` (kind∈transcript/ocr, ref_id, span), `Claim` (text + entities + source_status + **evidence≥1** — a claim MUST cite evidence), `RhetoricalBeat`/`VisualBeat` (kind + span + claim_ids), `VideoBlueprint`. Registered `video-blueprint` → schemas + TS + v1 compat + negatives. `domain/blueprint.py` (pure): `build_candidate_rhetorical_beats` tiles [0,duration] seamlessly from transcript segments, all UNCLASSIFIED — **the PROGRAM provides candidate time ranges so the LLM can't fabricate timestamps** (§10.2); `build_visual_beats` (VLM labels → VisualBeat kinds); and **`validate_blueprint` — the guardrail** turning §10.2 rules into code: times within [0,duration], rhetorical beats monotonic, every Claim cites evidence whose ref_id resolves to a real transcript-segment/text-track (transcript↔segment_ids, ocr↔track_ids), evidence within duration, beat.claim_ids reference real claims, rhetorical coverage (interval UNION not sum) ≥ 0.9 — returns typed `BlueprintIssue`s; plus `is_valid_blueprint`/`coverage_ratio`. `provider-sdk/blueprint_fusion.py` (contracts-only — candidates come IN via the request, so NO domain dep): `BlueprintFusionProvider` port + `UnconfiguredBlueprintFusionProvider` (honest) + `FakeBlueprintFusionProvider` (first→HOOK/last→CTA, middle stays UNCLASSIFIED — honest, doesn't fake classification; one Claim citing a real segment; produces a blueprint passing `validate_blueprint` with zero issues, never fabricates timestamps outside the candidate set). End-to-end test: transcript+visual → domain candidates → Fake fuse → validate → []. Both candidate/visual builders clamp/skip upstream times that exceed the target duration (verifier-caught boundary bugs, fixed + regression-tested). Guardrail detection boundary (documented): it checks evidence ref_ids + span-in-duration but doesn't cross-check the span against the referenced segment's real times. LLM fusion registered L2, deferred. **P2 is COMPLETE** (VF-201…VF-205). Deferred across P2: real ASR/OCR/VLM/LLM engines + models, and the AnalysisWorkflow that persists Transcript/TextTrackSet/VisualAnalysis/VideoBlueprint + wires stages with cache-keyed local re-run (VF-201 `activity_cache_key`). → **P2 Exit acceptance gate** (`tests/acceptance/`, report `docs/implementation/54b-p2-exit-report.md`): a runnable suite wired into pytest `testpaths`. Blueprint traceability: **22 synthetic zh/en/mixed samples** run the full P2 pipeline (Fake ASR transcript + OCR text-tracks + VLM visual-analysis → domain candidates + visual-beats → `FakeBlueprintFusionProvider`) → **22/22 pass `validate_blueprint` with 0 issues, all 22 Claims trace to a real transcript-segment/text-track, every beat time comes from the program-provided candidates (no fabricated timestamps)**. Cache/local-re-run: a `CachedStage` keyed by VF-201 `activity_cache_key` — same input+provider+model+config → cache hit, underlying provider runs once (no re-download/re-compute); changing input/model/config → recompute. Verifier independently confirmed the guardrail is not a decoration (tampering evidence ref → EVIDENCE_REF_INVALID, beat overrun → TIME_OUT_OF_RANGE, ghost claim → BEAT_CLAIM_REF_INVALID all fire) and that the acceptance changes touched NO src. **P2 is CLOSED.** Deferred (honestly listed): real ASR/OCR/VLM/LLM engines + weights, audio-fingerprint/diarization models, and the AnalysisWorkflow persisting Transcript/TextTrackSet/VisualAnalysis/VideoBlueprint with cache-keyed local re-run. → **VF-301 Creative Brief + Claim Table** (P3 start — understanding → creation): `contracts-py/brief.py`: `CreativeOpportunity` (trend_cluster + blueprint + rationale + target platform/language — the trend→creation handoff), `CreativeBrief` (objective/audience/platform/duration_target_ms>0/creation_mode/angle/`BriefHook`/must_cover_claim_ids/avoid/`VisualMix`/cta, per doc 42 §2; `VisualMix`'s 4 shares validated to sum ≈1), `ClaimTableEntry` (fact_status=`ClaimSourceStatus`, evidence≥1, confidence, recency, allowed_phrasings, usable_in_rewrite) + `ClaimTable`. Registered `creative-opportunity`/`creative-brief`/`claim-table` → schemas + TS + v1 compat + negatives. `domain/brief.py` (pure, id/created_at passed in): `build_claim_table` extracts a VideoBlueprint's claims into a ClaimTable carrying fact_status/evidence, dedups by text, and marks **DISPUTED claims `usable_in_rewrite=False`** (must be human-verified; per §2 model-generated new facts default UNVERIFIED and can't auto-pass before publish); `build_creative_brief` assembles a Brief from an Opportunity + creative params, defaulting `visual_mix` by creation_mode and inheriting platform/language from the Opportunity; and **`validate_brief` — the guardrail**: must_cover_claim_ids must reference real ClaimTable entries, and can't force-cover a DISPUTED or not-usable claim (→ typed `BriefIssue`s). Verifier CONFIRMED (37 adversarial assertions: VisualMix sum can't be bypassed, extraction dedup/fact-status correct + no input mutation, guardrail all 3 issue kinds fire, no clock, dep-direction clean, schema no-drift). Deferred: LLM angle/hook suggestion, Brief/ClaimTable persistence + wiring into the creation workflow. → **VF-302 Structure Rewrite Engine** (P3 creation: understanding → script): `contracts-py/rewrite.py` (registered `beat-template` + `script-version`): `BeatSlot` (role=`RhetoricalBeatKind`, start/end/target_duration/guidance, end≥start), `BeatTemplate` (source_blueprint_id/duration_target_ms>0/slots), `ScriptSentence` (beat_slot_id/role/text/target_duration/claim_ids/language/editable), `ScriptVersion` (brief/template/claim_table refs + version + sentences + total_duration). `domain/rewrite.py` (pure): `estimate_duration_ms` (zh non-space chars/5, en words/2.5 — speech-rate, not char-count §3.2), `build_beat_template` (blueprint rhetorical beats scaled proportionally to the target duration into seamless `BeatSlot`s, last slot absorbs rounding to end EXACTLY at target, `guidance`=beat.summary — **function only, never source verbatim**; empty beats → one UNCLASSIFIED slot filling the target), and **`validate_script` — the guardrail**: anti-copy (script sentence vs source transcript segments via punctuation-stripped normalize + `text_simhash` hamming≤6), facts (every cited claim must be in the ClaimTable and not DISPUTED/unusable), duration (sum of sentence target_durations within ±15% of brief budget), banned phrases (brief.avoid), must_cover coverage → typed `ScriptIssue`s; `is_valid_script`. `provider-sdk/rewrite.py` (contracts-only, no domain dep — candidates/table come IN via the request): `StructureRewriteProvider` port + `UnconfiguredStructureRewriteProvider` (honest UNCONFIGURED, `script=None` — never fakes a success) + `FakeStructureRewriteProvider` (deterministic neutral role-template sentence per slot, distinct-from-source; attaches usable must_cover claims to the longest-duration slot; each sentence.target=slot.target so total=budget; produces a script passing `validate_script` with zero issues; **never cites a DISPUTED/unusable claim even when `must_cover` demands it** — the guardrail then flags MUST_COVER_NOT_ASSERTED, since such a brief is itself invalid per `validate_brief`). Real rewrite LLM registered L2 in `third_party_manifest.yaml` (stop-condition deferral). fable verifier CONFIRMED template arithmetic (308 cases: gaps/overlaps/zero-duration beats/target≪source — every slot contiguous, target_duration≥0, last slot ends exactly at target), Fake always passes the guardrail, disputed-fact never cited, layering clean, schema no-drift — but REFUTED one **safety false-negative**: the anti-copy check was evadable by inserting a single punctuation mark mid-sentence (char-3-gram simhash is highly insertion-sensitive and `_normalize` didn't strip punctuation, so a verbatim copy + one full-width comma scored hamming 17 and passed). **Fixed**: `_normalize` now strips all Unicode punctuation (P*, covers zh+en) and simhash is computed over the normalized text, so punctuation/segmentation laundering collapses to a norm-equal match; regression tests reproduce the exact zh/en bypasses + a probe confirms 5 laundering variants all caught while genuine rewrites stay at hamming 30–33 (zero false positives). Note: test dirs still can't share module basenames (domain + provider-sdk both had `test_rewrite.py` → pytest import collision → provider side is `test_rewrite_port.py`). Deferred: real rewrite LLM + weights, Brief/ClaimTable/ScriptVersion persistence + wiring into the creation workflow. → **VF-303 Highlight Ranker** (P3 creation: the SOURCE_REEDIT segment-selection engine): `contracts-py/highlight.py` (registered `highlight-set`): `HighlightFeatures` (the §5.2 eleven sub-scores each ∈[0,1]), `HighlightWeights` (template-versioned, magnitude coefficients ≥0 — the two penalty terms' negative sign lives in the formula, defaults match §5.2), `HighlightCandidate` (time window + features + score + reason_codes + human_label + nullable `predicted_retention`), `HighlightSet`; enums `HighlightReason`/`HighlightLabel`. `domain/highlight.py` (pure): `build_candidate_windows` (windows on transcript sentence boundaries, duration ∈[min,max] — short-video 5–30s / long-interview 20–90s / default 12–75s — **never cuts a sentence**: the window end is `cover_end` = the farthest end among included segments, so even diarized-crosstalk overlapping segments (a later segment ending earlier) are fully contained, never truncated; a single segment longer than max yields no window); `highlight_score` (the §5.2 weighted formula, coefficients from the weights template, penalty terms subtract, bit-reproducible); `derive_highlight_reasons` (threshold→reason codes; **renamed from `derive_reason_codes` to avoid shadowing VF-101 trend's function of that name** — both coexist in `videoforge_domain`); `overlap_ratio` (intersection/shorter, containment→1.0) + `rank_highlights` (dedup windows overlapping >0.7 keeping the higher score, then MMR balancing normalized score vs max(temporal-overlap, feature-cosine) similarity → Top-N, deterministic stable-sort, reproducible). `provider-sdk/highlight.py` (contracts-only, no domain dep): `HighlightFeatureProvider` port + `UnconfiguredHighlightFeatureProvider` (honest UNCONFIGURED, `features=[]`, never fabricates) + `FakeHighlightFeatureProvider` (deterministic features from window position/duration/text-hash, aligned 1:1 with windows, earlier→higher hook / later→higher ending_payoff, **never sets `predicted_retention`**, no shared mutable state). The 11 features are model judgments → real VLM/LLM/trained model is a stop-condition deferral (`third_party_manifest.yaml` L2 entry); the scoring/dedup/MMR are pure deterministic domain; `predicted_retention` stays null without training data (§5.2, not faked). fable verifier CONFIRMED the §5.2 formula (200 vectors bit-exact), MMR normalization (no div-by-zero on equal scores) + determinism, the Fake never fabricating retention / leaking state, the name-collision fix (no shadowing), layering + schema no-drift — but REFUTED the "never cuts a sentence" guarantee under **overlapping segments** (a contract-valid diarized transcript where a later segment ends earlier produced a window truncating an included long sentence). **Fixed**: window end is now `cover_end`=max end of included segments (non-overlapping transcripts unchanged); overlap regression test + probe confirm both of the verifier's counterexamples yield zero truncations. Note: test dirs still can't share basenames (`test_highlight.py` domain + `test_highlight_port.py` provider). Deferred: real feature model + weights, HighlightSet persistence + wiring, and richer MMR topic/person/visual-embedding diversity (currently a feature-cosine proxy). → **VF-304 Source Re-edit Planner** (P3 creation: the SOURCE_REEDIT counterpart to VF-302): `contracts-py/reedit.py` (registered `reedit-plan`): `EditOp` (op=`EditOpKind` KEEP/DELETE/MUTE/SPEED/REFRAME, source_start_ms/end_ms with end≥start, segment_ids, output_order for KEEP reordering, reason, speed>0, reframe), `ReframeHint` (target_aspect_ratio + `ReframeFollow` SPEAKER/PRODUCT/UI/FACE/CENTER), `ContinuityNote` (`ContinuityRuleKind` J_CUT/L_CUT/INSERT_BROLL/JUMPCUT_SMOOTH/BEAT_ALIGN + at_ms + detail), `ReeditPlan` (ops + continuity + kept_duration_ms), `SegmentJudgment` (provider-produced DTO for "which segments to drop": is_silence/is_filler/is_repeat/is_low_info + keep_recommended — unregistered, not persisted). `domain/reedit.py` (pure): `build_reedit_plan` — **overlap-aware clustering** by `cover_end` (VF-303 precedent): diarized crosstalk / contained segments cluster into one EditOp, with **OR-semantics** inside a cluster (any segment kept → the whole cluster KEEP, since you can't cut audio for one overlapping speaker without cutting the other; all-drop → single DELETE with the priority reason silence>filler>repeat>low_info). KEEP ops get sequential `output_order` in source order and the ReframeHint; adjacent kept spans not touching in source → auto emit one JUMPCUT_SMOOTH `ContinuityNote` whose detail is source-neutral ("输出不连续", covers both delete gaps and natural source gaps). `validate_reedit_plan` — the §4.2 guardrail: EMPTY_PLAN, MID_SENTENCE_CUT (op boundary must be in segment starts/ends AND **strictly outside every segment's interior** — the flat-boundary check alone is fooled by overlapping segments), RANGE_OUT_OF_BOUNDS (duration = max(reported duration_ms, last segment end) — contract allows the two to disagree), KEEP_OVERLAP, INVALID_OUTPUT_ORDER (0..k-1 permutation), SPEED_OUT_OF_RANGE (default [0.5,2.0] inclusive), UNHANDLED_JUMPCUT (a discontinuity between adjacent kept spans requires a J_CUT/L_CUT/INSERT_BROLL/JUMPCUT_SMOOTH note at the earlier span's end) → typed `ReeditIssue`; `is_valid_reedit_plan`. `provider-sdk/reedit.py` (contracts-only, no domain dep): `SegmentJudgeProvider` port + `UnconfiguredSegmentJudgeProvider` (honest UNCONFIGURED, `judgments=[]` — never fabricates) + `FakeSegmentJudgeProvider` (deterministic: empty/whitespace → is_silence, filler-word set → is_filler case-insensitive, text equal to the previous KEPT segment's text → is_repeat, else keep; 1:1 with request, no shared state). Real segment-judge model registered L2 in `third_party_manifest.yaml` (stop-condition deferral). fable verifier CONFIRMED 3000-case fuzz over non-overlapping transcripts (build always passes its own validate), all 7 guardrails' true-positive + clean-plan true-negative (KEEP_OVERLAP not fired on exact-touching a.end==b.start, SPEED at 0.5/2.0 inclusive, RANGE_OUT_OF_BOUNDS with duration_ms=None), Fake determinism + repeat-vs-previous-kept-text, layering + schema no-drift — but REFUTED three properties, **all under contract-valid overlapping-segment inputs** (same class as the sibling VF-302/VF-303 refutations): (1a) built plan fails its own validate with KEEP_OVERLAP + double-counted kept_duration when overlapping segments both default-keep; (1b) built plan flagged RANGE_OUT_OF_BOUNDS when reported `duration_ms` < max segment end; (2) MID_SENTENCE_CUT missed by hand-built plans whose endpoints belong to the flat starts/ends sets but strictly split another overlapping segment; plus a factually-wrong "删除 [x,y) 后…" detail text for natural source gaps. **Fixed**: build clusters overlapping/contained segments by cover_end with OR-semantics so each cluster is a single EditOp and kept_duration never double-counts; validate takes `duration = max(reported, max_seg_end)`; MID_SENTENCE_CUT now requires each boundary to not strictly fall inside any segment; continuity detail text no longer claims a cause. Regression tests reproduce all three verifier counterexamples + a probe confirms mixed-keep-drop overlap collapses to one KEEP (protective), all-drop cluster collapses to one DELETE with priority reason, and natural source gaps produce a source-neutral JUMPCUT note. Note: test dirs still can't share basenames (`test_reedit.py` domain + `test_reedit_port.py` provider). Deferred: real segment-judge model + weights, ReeditPlan persistence + wiring into the creation workflow, richer continuity policies (J-cut/L-cut/INSERT_BROLL beyond auto-JUMPCUT). → **VF-305 Asset Resolver** (P3 creation: assets for the timeline): `contracts-py/asset_plan.py` (registered `asset-plan`): `CompositionSpec` (aspect_ratio + safe_area), `AssetLicense` (`AssetLicenseType`=OWNED/LICENSED_STOCK/ROYALTY_FREE/PUBLIC_DOMAIN/GENERATED_MODEL/PLACEHOLDER + holder + valid_until nullable + notes — the compliance-audit payload), `AssetPlanSlot` (time range + role=`AssetRole` + query + semantic_requirements + composition + **allowed_sources non-empty** + fallback), `ResolvedAsset` (**must carry source + license + query + usage span** — §6.2 provenance four-tuple — plus match_score∈[0,1] + reuse_count ≥1 + is_fallback), `AssetPlan`. Enums `AssetRole`/`AssetSource`/`AssetLicenseType`. `domain/asset_plan.py` (pure): `RESOLVER_PRIORITY = (SOURCE, OWN_LIBRARY, STOCK, GENERATED, PLACEHOLDER)` — the strict §6.2 five-tier order (authorized source → own library → licensed stock → AI generated → placeholder); `match_score` (§6.2 weighted: semantic 0.28 + composition 0.18 + resolution 0.14 + motion 0.10 + color 0.10 + brand 0.20 − reuse_penalty 0.10 × reuse_count, clamped [0,1], `DEFAULT_MATCH_WEIGHTS` module-level singleton — bit-reproducible); `resolve_asset_plan` (walks RESOLVER_PRIORITY for each slot, picks highest-score candidate ≥ threshold within `slot.allowed_sources`, tie-break asset_id ascending, tracks cross-slot reuse_count so a same asset picked again incurs extra penalty, falls back to a PLACEHOLDER `ResolvedAsset` with `is_fallback=True` when nothing qualifies); `validate_asset_plan` five guardrail issues: UNRESOLVED_SLOT / SOURCE_NOT_ALLOWED (**with `is_fallback` exempt — fallback is the designed escape hatch to human review, so a placeholder resolution isn't a compliance violation; after replacement the real asset will naturally satisfy allowed_sources**) / LICENSE_INSUFFICIENT (only fires when both `valid_until` and `now` given, strict `<`) / USAGE_OUT_OF_SLOT / REUSE_EXCEEDED (default max 3). `provider-sdk/asset.py` (contracts-only, no domain dep): `AssetSourceProvider` port (one provider per source tier) + `UnconfiguredAssetSourceProvider` (honest UNCONFIGURED, `candidates=[]` — never fabricates a license) + `FakeAssetSourceProvider` (deterministic pool indexed by role; **deep-copies each license on BOTH construction and return** so a caller can't pollute the internal pool by later mutating their license object — same defense as VF-203 OCR). Real asset sources (own vector library / commercial Stock / AI generation / avatar) registered L2 in `third_party_manifest.yaml` (stop-condition deferral). fable verifier CONFIRMED priority strictness (SOURCE wins over higher-scoring OWN_LIBRARY), allowed_sources enforcement, threshold-boundary 0.5 accepted, cross-slot reuse penalty shifts picks and 1-indexes `reuse_count`, all 5 guardrails true/false-positive, output-side license deep-copy, Fake never fabricates, layering + schema no-drift — but REFUTED one honesty defect: `FakeAssetSourceProvider` shallow-copied the input pool, so a caller mutating their original `AssetLicense` (Pydantic ContractModel, not frozen) after construction corrupted the internal pool and all subsequent `search()` results (docstring claimed input-side deep-copy but the code only did `{role: list(specs) ...}`); plus two seams — fallback plans failing their own guardrail with SOURCE_NOT_ALLOWED (since PLACEHOLDER ∉ allowed_sources), and a dead `plan_created_at` param. **Fixed**: `FakeAssetSourceProvider.__init__` now does `replace(spec, license=spec.license.model_copy(deep=True))` per spec at construction; `validate_asset_plan` explicitly exempts `is_fallback=True` from SOURCE_NOT_ALLOWED with a comment stating the design intent; dead param removed; 2 regressions cover both (post-mutation pool integrity + fallback plan passes validate); a probe reproduces the verifier's exact license-tampering counterexample and shows the internal pool now stays `orig`. Note: test dirs still can't share basenames (`test_asset_plan.py` domain + `test_asset_port.py` provider). Deferred: real asset sources + own-library semantic vectors + Stock/GENERATED providers, AssetPlan persistence + workflow wiring, and cleanup/watermark handling per §7 (VF-309/QA). → **VF-306 CreativeTimeline + OTIO** (P3 creation capstone — the timeline is what all upstream work compiles into): `contracts-py/timeline.py` (registered `creative-timeline`): `TrackKind` 11 values (V0_BACKGROUND / V1_PRIMARY_VIDEO / V2_BROLL_SCREEN / V3_INFO_CARDS / V4_CAPTIONS / V5_OVERLAYS / A0_ORIGINAL / A1_DUB / A2_MUSIC / A3_SFX / M0_MARKERS) one-to-one with §8.1; `RationalTime` (value≥0 + rate>0, OTIO-compatible) + `RationalTimeRange` (start.rate == duration.rate); `Segment` carrying **nine extension fields** — source_ref / semantic_role / script_sentence_id / speaker_id / provenance_ref / template_slot / effects / crop_path / localization — forming a source→transcript→script→template→asset→language traceability chain; `Track` + `CreativeTimeline` (rate + duration + tracks + source refs; duration.rate == rate). `domain/timeline.py` (pure): `validate_timeline` six-issue guardrail — EMPTY_TIMELINE / REQUIRED_TRACK_MISSING (V1 primary video must exist AND at least one of A0_ORIGINAL/A1_DUB, both derived from tracks with non-empty segments) / RATE_MISMATCH / TIME_OUT_OF_RANGE (touching-end allowed) / SEGMENT_OVERLAP (within same track, touching a.end==b.start allowed; cross-track never flagged) / INVALID_PROVENANCE_REF. `to_otio_mapping` / `from_otio_mapping` — pure dict OTIO structure (**does not import opentimelineio** — the pip package integration is deferred to a downstream Exporter layer); nine extension fields land in `metadata.videoforge` with bit-exact round-trip; **duration is explicitly carried in `metadata.videoforge.duration_value`** so padded timelines (where source `duration > max(segment end)`) preserve their trailing whitespace on round-trip (falls back to max(end) when the field is absent, so externally-produced OTIO dicts still read). `provider-sdk/timeline_exporter.py` (contracts-only, no domain dep): `TimelineExporter` port + `UnconfiguredTimelineExporter` (honest UNCONFIGURED, `payload={}`) + `OtioDictExporter` (**takes an injected mapper** — the caller passes `domain.to_otio_mapping` — so provider-sdk src never imports `videoforge_domain`; real `.otio` file writes deferred to VF-310). OpenTimelineIO registered L0 Apache-2.0 in `third_party_manifest.yaml` (only runtime integration is deferred, the pure dict mapping needs no pip package). **ADR-003 binding**: `CreativeTimeline` is the domain truth — OTIO/FCPXML/剪映 are only interchange, they don't save media and don't cover all advanced motion effects; Render Graph goes to the renderer (VF-307 FFmpeg / VF-308 Remotion). fable verifier **CONFIRMED all claims** under adversarial probing — validator six kinds (empty / required-track / rate / range / overlap / provenance all fire and don't false-fire on touching/cross-track/None cases), OTIO round-trip preserves all nine extension fields incl. LocalizationPolicy + SegmentEffect with mixed int/float/bool/str params + M0 marker track + zero-duration segment (byte-exact `model_dump()` equality), layering clean (docstring mention only), schema zero-drift, 607 passed. Three non-refuting observations flagged and addressed: (a) `from_otio_mapping` used to drop trailing padding via max(end) — **fixed** by explicitly carrying `duration_value` in `metadata.videoforge`; (b) SEGMENT_OVERLAP adjacent-pair check only lists the first pair per track — issue-list simplification but `is_valid_timeline` is provably complete (sort invariant); (c) RATE_MISMATCH `continue` also skips provenance — same class, `is_valid` unaffected. Added comments on (b)/(c) documenting the "is_valid complete, issue list simplified" design; 2 regressions for (a) — padded-duration round-trip preserves 150 not 100, missing-field falls back to max(end). Note: test dirs still can't share basenames (`test_timeline.py` domain + `test_timeline_exporter.py` provider). Deferred: real `.otio` file writes (opentimelineio pip integration, VF-310 Exporters); FCPXML / 剪映 / CapCut mappings (VF-310); RationalTime rate normalization when different source clips carry different rates (currently rejected; may need per-clip rate conversion). → **VF-307 FFmpeg Compiler** (P3 final — CreativeTimeline compiled to reproducible ffmpeg argv): `contracts-py/render.py` (registered `render-manifest`; enums `RenderStage`=PROXY/FINAL and `RenderTargetKind`=MP4_H264/MP4_H265/MOV_PRORES/WEBM_VP9): `RenderInput` (asset_id + sha256 64-hex validator + resolved_path with shell-metachar rejection + role); `FilterNode` (id + filter with regex `[a-zA-Z][a-zA-Z0-9_]*` + params dict[str,str] with metachars rejected on both keys and values + inputs + outputs min_length=1); `FilterGraph` (nodes + sinks, model_validator rejects duplicate output labels); `FfmpegRenderGraph` (**args strict list[str] min_length=1 with each token non-empty AND every shell metachar `;|&<>` backtick `$\\"'\n\r\t` rejected** + filter_complex FilterGraph + inputs + output_path metachar-rejected + target=RenderTargetKind + tool_version); `RenderManifest` (timeline_id + stage + render_graph + input_digests dict{asset_id: sha256} + output_digest + tool_version). `domain/ffmpeg_compiler.py` (pure): `compile_timeline(timeline, resolved_inputs, config, tool_version)` — walks V1_PRIMARY_VIDEO / V2_BROLL_SCREEN / A0_ORIGINAL / A1_DUB tracks, generates per-Segment sub-DAG (video: trim → setpts → scale → setsar; audio: atrim → asetpts → aformat), auto-`concat` for ≥2 segments per track, dedupes `-i` across tracks that share a source_ref, no `-filter_complex` string ever enters `args` (serialization is media-core executor's job — the compiler stops at structured FilterGraph); `build_render_manifest` + `render_manifest_cache_key` = SHA256 over stage|target|tool_version|sorted(input_digests)|argv joined by `\x00` — bit-reproducible, changes on any input hash or tool_version change; `serialize_filter_complex` for downstream executor. **Security-critical `_path_within` uses stack-based `..` normalization** (segments walked; `..` pops the stack; result compared against roots lexically) — no `os.path.realpath` (avoids TOCTOU / symlink pitfalls); rejects: empty roots, relative paths, over-popped `..`, and prefix-match false positives like `/stagingextra` matching `/staging`. `UnsafeInputPath` raised on any source_ref not in `resolved_inputs`, any input path outside whitelist, and any output_path outside whitelist. **ffmpeg registered L3 in `third_party_manifest.yaml`** (already established VF-201) with tool_version probed and threaded through every manifest for replay. fable verifier CONFIRMED the argv shell-metachar defense holds across every input channel (resolved_path with `;rm -rf /`, source_ref, config fields all get rejected at the contract or domain layer; source_ref never reaches argv/filter params, only synthesized integer indices and `v1_N` label prefixes do); confirmed 180 targeted tests, zero schema drift, layering clean — but **REFUTED one safety-critical defect** — `_path_within` originally used `PurePath.relative_to` which is purely lexical and does NOT collapse `..` components. Reproducible attack: `/staging/../../etc/passwd` passed the whitelist and reached ffmpeg argv verbatim, letting the renderer read arbitrary paths; same class for output_path allowing arbitrary writes. **Fixed**: segment-level stack folding normalization before `relative_to`; three regression tests reproduce the exact `..` variants (`/../etc/passwd`, `/a/../../etc/shadow`, `/./../../root/.ssh/id_rsa`), and a probe confirms benign `./nested/x.mp4` and `/stagingextra/foo` prefix cases classify correctly. Note: this is the same class of "safety-critical fix on top of a passing test suite" as VF-303 mid-sentence-cut and VF-305 Fake license leak — verifier's job proven again. Note also: test dirs still can't share basenames (`test_ffmpeg_compiler.py` is domain-only, no provider variant). Deferred: media-core RenderRuntime that actually executes the FfmpegRenderGraph (builds argv from filter_complex + calls ffmpeg subprocess via VF-201 FfmpegRuntime primitives) and Golden Media Tests (rendering testsrc → hash-compare across runs); real font/subtitle filters; V3 overlays and V5 brand watermark; audio mixing (amerge) beyond concat; provider-sdk RenderProvider port for GPU-worker dispatch. → **VF-308 Remotion Compiler** (P3 completion — the JS/React counterpart to VF-307): `contracts-py/remotion.py` (registered `remotion-render-manifest`; enum `RemotionComposition`=CAPTIONS/INFO_CARD/DATA_CHART/BRAND_ANIMATION/LOWER_THIRD): `RemotionProp` (key JS identifier regex + value **recursive JSON-compat validator with depth ≤8 and every string value/dict key metachar-rejected `;|&<>` backtick `$\\"'\n\r\t`** — nested dicts of dicts of list-of-dicts all traversed); `RemotionRenderRequest` (timeline_id + composition + props with unique keys + duration_ms≥0 + fps∈(0,120] + width/height∈(0,8192] + entry_component_path metachar-rejected + output_path metachar-rejected + tool_version); `RemotionRenderManifest` (request + input_digests 64-hex + output_digest optional 64-hex + tool_version + created_at). `domain/remotion_compiler.py` (pure): `_COMPOSITION_TO_TRACK_KINDS` — CAPTIONS/LOWER_THIRD → V4_CAPTIONS; INFO_CARD/DATA_CHART → V3_INFO_CARDS; BRAND_ANIMATION → V5_OVERLAYS; `compile_timeline_to_remotion` walks each mapped track and emits per-segment structured entries (segment_id / start_ms / end_ms plus optional script_sentence_id / speaker_id / template_slot); **entry_component_path and output_path both go through `_path_within` imported from VF-307's `ffmpeg_compiler`** — same segment-level `..` stack-folding normalization, same whitelist enforcement (fixed once, applies twice); `serialize_props` produces canonical JSON (sort_keys, separators, ensure_ascii=False) for cache reproducibility; `remotion_manifest_cache_key` = SHA256(composition | tool_version | entry_component_path | fps | width | height | duration_ms | sorted digests | canonical props) — sensitive to props/tool_version/paths, not to manifest_id. `provider-sdk/render_provider.py` (contracts-only, does not import domain): `RenderProvider` Protocol + `UnconfiguredRemotionRenderProvider(license_pending=True)` (**Remotion commercial license red line**: default returns `LICENSE_PENDING` + `manifest=None` — the architecture explicitly says "license not cleared, can't use") + `FakeRemotionRenderProvider` (**strictly does NOT import any remotion.* module or spawn subprocesses**; `output_digest=None` unless caller pre-records via `record_output(request_id, digest)`; `render_count` counter for tests). `third_party_manifest.yaml` L2 entry for Remotion states the license-review red line and ADR-002 pointer. fable verifier **CONFIRMED all claims** — 70+ adversarial probes over Prop metachar defense (depth 8 accepted, depth 9 rejected; each of the 13 metachars rejected individually; bytes/set/datetime rejected; non-str dict keys rejected; `model_validate` JSON round-trip path also rejected — the validator runs at construction AND reconstruction), path whitelist inherits VF-307's fix verbatim (`/staging/../../etc/passwd` rejected for both entry and output paths; `/stagingextra/e.tsx` prefix-bypass rejected; empty roots rejects everything; benign `/staging/./nested/e.tsx` accepted), composition-to-track semantics (CAPTIONS on V3-only timeline gives `captions: []` — honest empty, not fabricated), cache key sensitivity across every input, Fake red line by `sys.modules` diff (zero remotion.* imports) + grep (no subprocess/os.system/Popen/create_subprocess), Unconfigured honesty (both LICENSE_PENDING and UNCONFIGURED return `manifest=None`), layering clean, schema zero-drift, all 8 negatives fire, 663 passed. One non-refuting observation: nested `dict` **in-place mutation** on an already-validated `RemotionProp.value` bypasses Pydantic revalidation and reaches `serialize_props` — but this is a **repo-wide Pydantic posture** (VF-307's `FfmpegRenderGraph.args.append(";rm")` leaks the same way; `validate_assignment=True` catches direct reassignment but not deep in-place mutation); noted as a defense-in-depth reminder for the future real executor boundary (revalidate the request at render time), not a VF-308 regression. Note: test dirs still can't share basenames (`test_remotion_compiler.py` domain + `test_render_provider.py` provider). Deferred: real Remotion runtime (license review + Node/npm + React sub-package + subprocess adapter); Remotion Player preview in the Web app; V2 broll composite via Remotion (currently ffmpeg territory); GPU-worker RenderProvider variant for cloud dispatch. **P3 creation/editing is now feature-complete for the "planning → timeline → render graph" axis** — VF-301..VF-308 all done. → **VF-309 QA Gates** (P3 pre-publish safety gate): `contracts-py/qa.py` (registered `qa-report`; enums `QASeverity`=BLOCKER/MAJOR/MINOR/INFO and `QAFindingKind` 16 kinds covering video/audio/caption/composition/duration): `QAFinding` (id + kind + severity + at_ms/duration_ms + track_ref + evidence dict), `QAReport` (findings **with unique-id validator** + timeline/measured durations + pass_or_block bool + **cross-integrity validator that rejects `pass_or_block=True` alongside any BLOCKER finding** — hand-built reports can't lie past the domain publish gate; the publish flow must call `validate_publish_gate` to compute the bool). `domain/qa.py` (pure, zero I/O): `QAThresholds` dataclass (black_luma_max=8.0, frozen_min_frames=6, loudness ±2 around -14 LUFS, true_peak_max=-1 dBTP, safe_area 5–95%, broll_min_ratio=0.15, duration_tolerance=100ms, max_major_before_block=3) + `DEFAULT_THRESHOLDS` singleton; 9 rule functions (`detect_black_frames`/`detect_frozen_frames`/`detect_duplicate_frames`/`check_loudness`/`detect_voice_tail_cut`/`check_caption_safe_area`/`check_caption_overlap`/`check_broll_ratio`/`check_duration_match`) each returning typed `QAFinding` lists with **enumerate-indexed ids** so duplicate caption_ids from real analyzers can't crash aggregation; `aggregate_qa_report` runs all rules + sets pass_or_block; `validate_publish_gate` — **BLOCKER at any severity produces BLOCK**, MAJOR count > `max_major_before_block` upgrades to BLOCK, MINOR/INFO are report-only. `provider-sdk/media_analyzer.py` (contracts-only, no domain dep): `MediaAnalyzerProvider` Protocol + `UnconfiguredMediaAnalyzerProvider` (honest UNCONFIGURED, empty samples, never fabricates) + `FakeMediaAnalyzerProvider` (returns constructor-injected samples deterministically, **zero I/O, zero subprocess, zero ffmpeg/ebur128/opencv imports** — grep-verified). `third_party_manifest.yaml` L2 entry for ebur128/opencv/OCR detectors states the stop-condition deferral. fable verifier **CONFIRMED** all safety claims — 49 adversarial assertions across the "no BLOCKER slips to PASS" property (single-frame BLACK_FRAME, duration_ms=0 BLOCKER, 100-INFO-buried BLOCKER, TRUE_PEAK_CLIP in a non-longest window, `max_major_before_block=999999` with 500 MAJORs still passes but one BLOCKER still blocks); every rule's threshold boundary exactly matched (`≤` vs `<`, touching captions allowed, ratio equal-to-threshold passes, duration diff exactly 100 passes / 101 blocks); layering clean; schema no-drift. Three non-refuting observations, two of which were addressed: (a) duplicate `caption_id` from a real analyzer used to crash `aggregate_qa_report` on finding-id collisions — **fixed** with enumerate-indexed ids while keeping the unique-id contract; (b) `pass_or_block` was an unvalidated bool — **fixed** with the cross-integrity model validator on QAReport; (c) loudness is longest-window-only by design (documented in the docstring; short off-LUFS windows aren't flagged, but per-window True Peak still catches the BLOCKER case). 3 regressions cover duplicate caption_id + cross-integrity accept/reject. Note: test dirs still can't share basenames (`test_qa.py` domain + `test_media_analyzer.py` provider). Deferred: real ebur128 for LUFS/True Peak; real opencv for phash+luma; real OCR bbox; subject/UI crop VLM detectors; flicker/cleanup-zone detectors (currently only the 9 first-tier rules are implemented; the enum has all 16 kinds ready for the additional 7 detectors as they're built); MediaAnalyzer + QAReport persistence + wiring into a "QA before publish" workflow. **P3 creation/editing is now complete for planning → timeline → render → QA gate** — VF-301..VF-309 all done; only Exporters remain. → **VF-310 Exporters** (P3 收官 — NLE 交换资产写文件): `contracts-py/exporter.py` (registered `exporter-report`; enums `ExporterKind`=OTIO_FILE/FCPXML/JIANYING/CAPCUT and `ExporterStatus`=OK/PARTIAL/FAILED/UNSUPPORTED): `ExportEntry` (kind + status + output_path shell-metachars rejected + tool_version + warnings + errors + bytes_written), `ExporterReport` (aggregate). `domain/exporters.py` (pure, **no pip additions**): `write_otio_file` — pure JSON write of `OTIO_SCHEMA` (VF-306 `to_otio_mapping` serialized with `json.dumps`, byte-exact round-trip with `read_otio_file` + `from_otio_mapping` for all nine extension fields and padded duration); `write_fcpxml_file` — stdlib `xml.etree` FCPXML 1.10 minimal subset, each Segment becomes a `<clip>` in `<spine>`, **all nine extension fields (source_ref/semantic_role/script_sentence_id/speaker_id/provenance_ref/template_slot/effects/crop_path/localization + track_kind) land in a per-clip `<note>` as JSON** for DaVinci/Premiere round-trip retention; XML injection is auto-escaped by `ET.tostring` (a timeline id `<script>alert(1)</script>` becomes `&lt;script&gt;` in the file); `write_jianying_package` / `write_capcut_package` — experimental fallback per §10 底线: writes a directory containing `timeline.json` (full CreativeTimeline JSON) + `README.md` explicitly stating "MP4 已产出、导出失败不阻断发布", status=PARTIAL with a warning so the UI clearly signals "human import required"; `export_all` batches multiple exporters with **fault isolation** — any single failure (including `UnsafeInputPath`) is caught and recorded as a `FAILED` entry, never propagates to abort other exporters. `_guard_output_path` reuses VF-307's segment-level `..` folding whitelist **AND rejects shell metachars in the path BEFORE the write** — so `<tmp>/$(id).otio` is refused up-front instead of leaving a file on disk under a metachar name (verifier observation, addressed). Real `.otio` files are pure JSON per the OTIO_SCHEMA — no `opentimelineio` pip dependency needed for writes; the OTIO project's official reader can consume the output as-is (documented in VF-306 already). `third_party_manifest.yaml` entry for pyJianYingDraft/pyCapCut is L2 with the experimental red line and version-matrix caveat stated. fable verifier CONFIRMED path whitelist inheritance across all 4 writers (17 traversal cases), OTIO byte-exact round-trip + padded duration preservation, FCPXML structure + XML injection escaping, JianYing/CapCut PARTIAL contract with README naming §10, `export_all` fault isolation across mixed-outcome plans, layering (no `opentimelineio` import, no subprocess), and schema zero-drift — but REFUTED one claim: the FCPXML `<note>` originally carried only 6/9 extension fields (effects/crop_path/localization silently dropped). **Fixed**: `_timeline_to_fcpxml_root` now serializes all nine fields (effects as `[{kind, params}]`, localization as `{language, strategy}`, crop_path passthrough); 2 regressions cover the full-nine-field assertion + metachar-path-not-written-to-disk. Note: Apple's FCPXML spec expects integer rational times; current writer emits floats like `"150.0/30.0s"` — may need conversion for strict parsers in a follow-up. Deferred: real `pyJianYingDraft`/`pyCapCut` integration when licensing clarified; VLM-driven asset resolver for §7 cleanup workflows; workflow persistence tying `RenderManifest` → `QAReport` → `ExporterReport` into a single publish pipeline. **P3 全部完成 — VF-301..VF-310 十任务收官，创作→剪辑→时间线→渲染→QA→交换全链路 feature-complete。** → **P3 Exit 验收关口** (`tests/acceptance/`, report `docs/implementation/54c-p3-exit-report.md`). 覆盖 docs/modules/42 §12 七条: **① 两模式共蓝图产 Package** — 20 中英样本 (10 zh + 10 en, AI/non-AI 各半, 时长 30/45/60/90s, 含 VERIFIED/UNVERIFIED/DISPUTED/OPINION) STRUCTURE_REWRITE + SOURCE_REEDIT 各产 Package 全过 `validate_script`/`asset_plan`/`timeline`/`publish_gate` 四道 domain 护栏. **② 一句改动重跑范围（原语级）** — `activity_cache_key` 上游稳定/下游变/provider 命名空间隔离/config 敏感/tool_version 升级失效; workflow 级集成待 AnalysisWorkflow 落地. **③ Top-3 Highlight 命中率 ≥ 70%（覆盖率口径 |label∩topk|/|label|≥0.7）** — 10 fixture × 3 人工标注 = 30 应入选窗，跨集合 86.7%，额外抗抵消断言"无一条 fixture 0/3" (verifier 护栏加固). **④ PROXY vs FINAL 切点差 = 0 帧（结构级）** — `compile_timeline` 两 stage 下 trim/atrim `(filter, start, end)` 三元组完全一致（差异仅在 codec/preset 段: crf 28→20, preset veryfast→medium），-i/-map 段逐字符相同; 真实 GOP 对齐待 golden-media. **⑤ 20 样本零非预期 BLOCKER** — 注入合成 clean 媒体 (每 500ms 一帧 mean_luma=128 + phash 递增避免重复/冻结 + 合规居中字幕 + LUFS=-14 音频窗) 触发 9 条 QA 规则**真正跑起来**（非 vacuous 空输入）, 全 PublishGate.PASS. **⑥ OTIO 可导入 DaVinci（结构级）** — verifier 揭示的实质缺陷 (Clip 缺 `media_reference` 使 DaVinci 无法重连媒体, §12 第 6 条硬要求) **已修**: `_segment_to_otio_clip` 补 `ExternalReference.1 + target_url=source_ref` 或 `MissingReference.1`; `test_otio_file_has_expected_structure_for_davinci_import` 断言真落盘 + `from_otio_mapping` 往返无损 + schema 零 drift; DaVinci 客户端联合导入待 55 冒烟. **⑦ 剪映/CapCut 失败为 Adapter 限制、不破坏批量** — `export_all` 混合计划下 OTIO/FCPXML OK 不受 JIANYING/CAPCUT PARTIAL 或单个 FAILED 影响. **DISPUTED 关键安全属性** — `test_disputed_claims_never_forced_into_scripts` 首版 vacuous (事先把 DISPUTED 过滤出 must_cover 再断言 cited 无 DISPUTED, verifier REFUTED) → **修**: 直接构造 must_cover 强含 DISPUTED 的 brief 直调 Fake, 断言 script.sentences.claim_ids 与 disputed_ids 无交集——`FakeStructureRewriteProvider` 遇 DISPUTED must_cover 拒引是 VF-302 契约 (`usable_in_rewrite=False`) 的真实证明. fable verifier **两轮独立复审**: 首轮 6 项 findings (DISPUTED vacuous / cache_key 原语与生产落差 / no-BLOCKER vacuous / highlight docstring 与实现自相矛盾 / OTIO Clip 缺 media_reference 硬缺陷 / 切点差未标结构级) 全部修复; 二轮 6/6 CONFIRMED, 唯一 minor 残留 (§1 ② 未标"原语级") 已补. 跑 `tests/acceptance/` → 41 passed; 相关 domain/provider-sdk/contracts → 588 passed + schema_sync 25 passed. **P3 CLOSED.** Deferred (诚实登记): 真实 ASR/OCR/VLM/LLM 引擎 + 权重 (Fake 通验收护栏); AnalysisWorkflow 落地把 activity_cache_key 用起来; DaVinci 客户端联合导入 (55 macOS 冒烟); Golden Media Tests (真渲染 hash 比对, 需 media-core RenderRuntime 落地); 音频混合 (amerge) / V3 overlays / V5 水印 / 字体 / subtitles (VF-307 首版覆盖 V1/V2/A0/A1); 剪映/CapCut 官方 draft 格式 (许可未清). Git repo with trunk `main`; one task = one branch + `--no-ff` merge. `docs/` remains the implementation spec. Next: **P4 本地化** (VF-401 起 — timeline 翻译 + 双语字幕 + 配音; Blueprint + CreativeTimeline 已就绪, P4 只加语言维度不改结构). → **VF-401 Terminology + Canonical Localization** (P4 起步 — 术语 + Translate/Reflect/Adapt + Claim diff + 时长预算): `contracts-py/localization.py` (registered 4 new contracts: `canonical-script` / `glossary` / `translate-reflect-adapt-result` / `localization-variant`): `CanonicalScript`/`CanonicalSentence` (语言无关中性稿；每句 must_keep_terms + pronunciation_hints + visual_dependencies + edit_flexibility ∈ [0,1]；翻译修改不覆盖它，派生 LocalizedSentence 承载译文，§3), `Glossary`/`GlossaryEntry` (一对语言的术语表；preserve_source=True 强制 target_term == source_term；entries source_term 唯一；含 pronunciation 供 TTS), `TranslateReflectAdaptResult` (三阶段合同产出：draft/reflected_notes/adapted + alternatives + semantic_similarity ∈ [0,1] + duration_estimate_ms + claim_diff；changed_claims 非空**必须**人工审核，§4.3), `LocalizedSentence`/`LocalizationVariant` (每目标语言一份 Variant，共享 Canonical + Glossary；needs_review 由 domain 决定，provider 不直接置), `ClaimDiff`/`ClaimDelta` (结构化差异)。9 条 test_invalid 负例 + 4 个 v1 compat fixture + TS 生成 + schema 零漂移。`domain/localization.py` (纯函数、复用 VF-302 estimate_duration_ms): `build_canonical_script` (优先从 transcript 匹配 id 拿时间，否则 cursor 累加), `compute_claim_diff` (**只做纯 set diff** ADDED/REMOVED —— DISPUTED 由 validate 独立报告 DISPUTED_CLAIM_CITED，避免三重告警 —— verifier finding), **`validate_localization` 十种 issue** 组成本地化护栏: `EMPTY_VARIANT` / `SENTENCE_MISMATCH` (canonical 每句必须 variant 1:1 覆盖 —— 漏译 / 重复引用 —— verifier REFUTED 首版真空区后补加) / `UNKNOWN_CANONICAL_SENTENCE` / `LANGUAGE_MISMATCH` / `MUST_KEEP_TERM_LOST` / `GLOSSARY_VIOLATION` (preserve_source 术语被译走) / `CLAIM_DIFF_REQUIRES_REVIEW` / `DISPUTED_CLAIM_CITED` / `LOW_SEMANTIC_SIMILARITY` (<0.75) / `DURATION_OUT_OF_TOLERANCE` (相对目标 ±15%)。`_text_contains_term` 大小写不敏感 + word-boundary **仅 ASCII alnum** 生效 (verifier finding: 首版 `ch.isalnum()` 对 CJK 汉字返 True 导致 '这是AI芯片' 里的 'AI' 被误判为 word 一部分 → 报 MUST_KEEP_TERM_LOST 误报；修 `_is_word_char` 后 CJK 相邻 ASCII 术语正确命中，同时 'AI' 仍不在 'brain' 中命中)。`derive_review_flags` 按句聚合 issue.kind → 供上层置 `LocalizedSentence.needs_review` + `review_reasons`。`apply_glossary_preview` 长术语优先（'端侧' 先于 '端'，避免 'edge侧'），preserve_source 不动。`provider-sdk/localization.py` (contracts-only，无 domain 依赖): `TranslateReflectAdaptProvider` Protocol + `TRAStatus` (OK/NEEDS_REVIEW/FAILED/UNCONFIGURED) + `TRAErrorCode` (5 值) + `UnconfiguredTRAProvider` (恒返 UNCONFIGURED，真 LLM 延后属 stop-condition) + `FakeTRAProvider` (deep-copy in/out 不污染；术语表长优先 + preserve_source 不替 + 大小写不敏感精确替换；`claim_diff` **永远** src == loc 集合 —— Fake **决不构造 ADDED**，§4.3 安全底线；语言对 mismatch → FAILED + UNSUPPORTED_LANGUAGE_PAIR，不静默降级；semantic_similarity 固定 0.90 代表"直译+术语替换"中等信任；`_estimate_ms` 内联副本与 domain 数值逐位相等 —— zh 字/5 + en 词/2.5；must_keep 判定复用与 domain 同规则的 `_term_present`，避免 provider/domain 分歧)。fable verifier 独立复审 11 条主张：8 CONFIRMED，1 REFUTED (SENTENCE_MISMATCH 从未发射) + 2 observations (CJK-adjacent + DISPUTED 三重告警)，全部修复 + 3 条 regression (漏译 / 重复引用 / '这是AI芯片' AI 命中)。跑 packages/contracts-py + domain + provider-sdk 653 passed + 807 全套通过 + lint 全绿 + schema 零 drift。→ **VF-402 Subtitle Engine** (docs/modules/43 §5: 语义分段 + 阅读速度 + 安全区 + SRT/ASS/Timeline Overlay 三格式): `contracts-py/subtitle.py` (2 new contracts `subtitle-template` + `subtitle-track`): `SubtitleTemplate` (每行字符上限 + `max_lines_per_cue ≤ 2` + **CJK/EN CPS/WPM 分开配置**（§5.1 硬要求）+ min/max_cue_duration + `SafeAreaSpec` + `SubtitleStyleHint`；至少配一阅读速度否则拒), `SafeAreaSpec` (left<right / top<bottom 边界验证), `SubtitleLine`/`SubtitleWord`/`SubtitleCue`/`SubtitleTrack` (完整时间跨度 + 语言 + karaoke words + source_ref), `SubtitleFormat` enum (SRT / ASS / TIMELINE_OVERLAY). 9 条 test_invalid 负例 + 2 个 v1 compat fixture + TS 生成 + schema 零漂移. `domain/subtitle.py` (纯函数): `segment_text_into_lines` (中文按字符 / 英文按词贪心切；**数字+单位保护**用 `(?![A-Za-z0-9])` 断言而非 `\b`——首版 `\b` 在 CJK 相邻时失效, "20 ms结束" 被切开 —— verifier REFUTED 后改；`must_keep_terms` 术语保护), `pack_lines_into_cue` (多行合成 cue，超行合尾；按字符占比均分时间), `validate_subtitle_track` **十种 issue** 组成字幕护栏: `EMPTY_TRACK` / `CUE_ORDER_BROKEN` / `CUE_OVERLAP` (prev_start 与 prev_end 分开检测) / `CUE_TOO_SHORT` / `CUE_TOO_LONG` / `LINE_TOO_LONG` / `TOO_MANY_LINES` / `READING_SPEED_EXCEEDED` (CPS 按 line.language 决定规则 —— zh/ja/ko 走 CJK, 其余走英文；不看内容, verifier REFUTED 首版 `_is_cjk(text)` 使 en 行含 1 个 CJK 字即整行走 CJK 规则、若模板未配 cjk 阈值就跳过所有速度检查——护栏假阴性) / `LANGUAGE_MISMATCH` / `WORD_TIME_OUT_OF_LINE`. `render_srt` (标准 SRT，行内 `\n`), `render_ass` (ASS 头 + Style + Dialogue，颜色 hex→BGR，行内 `\N`), `to_timeline_overlay` (每 cue 一 dict → CreativeTimeline V4_CAPTIONS 上层适配), `render` 分派入口. `_words_for_line` 只按时间过滤（verifier obs: 首版 `w.text in line_text` 会把子串误纳 'ell'→'hello world' —— docstring 与实现矛盾, 修）. `provider-sdk/subtitle.py` (contracts-only): `ForcedAlignmentProvider` Protocol + `ForcedAlignmentStatus/ErrorCode` + `UnconfiguredForcedAlignmentProvider` (恒返 UNCONFIGURED, 真 whisperX/MFA/gentle 延后 stop-condition) + `FakeForcedAlignmentProvider` (zh 按字符 / en 按 whitespace 词, 按 token 长度占比在 [start,end] 均分, **每 word confidence 固定 0.5** 不假装高置信, `end<start`→FAILED, 空文本→OK+[], deep-copy 输入). fable verifier 独立复审 14 主张: 12 CONFIRMED + 2 REFUTED (数字单位保护 CJK 相邻失效 + CPS 语言分离可被单 CJK 字符击穿) + 1 minor obs (_words_for_line 子串误纳) 全部修复, 补 2 条 regression. 711 全套通过 + lint 全绿 + schema 零 drift. → **VF-403 On-screen Text Localization** (docs/modules/43 §6: TextTrack 策略 + Clean Plate + 布局跟踪 + 信息卡回退): `contracts-py/onscreen_text.py` (1 new contract `text-localization-plan`): `TextLocalizationStrategy` enum (REDRAW / REPLACE_OVERLAY / INFO_CARD_FALLBACK / KEEP_AS_IS / SKIP / LOCALIZE_ANNOTATION), `CleanPlateMethod` enum (BACKGROUND_ESTIMATE / AUTHORIZED_INPAINT / SKIP), `TextLocalizationReviewReason` 8 值人工审核原因, `TextLocalizationKindPolicy` (per-kind 默认策略 + 阈值 + fallback + license_check), `TextLocalizationPolicy` (per_kind 唯一 + max_occlusion + max_layout_expansion), `CleanPlateRequest` (**AUTHORIZED_INPAINT 必须携带 license_ref**——合同层硬拦，§README §4 授权/清理红线), `TextTrackLocalizationDecision` + `TextLocalizationPlan` (plan 内 clean_plate_request_id 必须解析到 requests、source_track_id 唯一——合同层 model_validator). 5 条 test_invalid 负例 + 1 v1 fixture + TS 生成 + schema 零漂移. `domain/onscreen_text.py` (纯函数): `decide_strategy` — **BRAND_MARK 硬红线**放在函数最前，无论 policy 怎么配都返 SKIP + review + LICENSE_UNCONFIRMED（policy 覆盖不了 §6.1 表格）；UNKNOWN → SKIP + UNKNOWN_KIND；kind_policy 缺失 → SKIP + review（安全默认，不静默 REDRAW）；UI (requires_license_check) → LOCALIZE_ANNOTATION + review；低置信 → fallback + LOW_OCR_CONFIDENCE；REDRAW 遮挡 > max 或 !allow_redraw → fallback + OCCLUSION_HIGH；layout 溢出 → fallback + LAYOUT_OVERFLOW. `plan_text_localization` 仅 REDRAW 生成 BACKGROUND_ESTIMATE CleanPlateRequest，其它策略无 Clean Plate. `validate_text_localization_plan` **十种 issue**: EMPTY_PLAN / `BRAND_MARK_ILLEGAL_STRATEGY` (BRAND_MARK 绕过 decide_strategy 手工构造 REDRAW/REPLACE_OVERLAY → 拦) / `UI_NEEDS_LICENSE_REVIEW` / UNKNOWN_KIND_NOT_ROUTED / KIND_POLICY_MISSING / CLEAN_PLATE_MISSING (REDRAW 缺 cp) / CLEAN_PLATE_UNJUSTIFIED (非 REDRAW 挂 cp) / LAYOUT_OVERFLOW (严格 > policy.max) / GLOSSARY_MUST_KEEP_TERM_LOST (preserve_source 术语丢失) / TRANSLATED_TEXT_MISSING / TRANSLATED_TEXT_ILLEGAL. `provider-sdk/onscreen_text.py` (contracts-only, 两端口): `CleanPlateProvider` Protocol + `UnconfiguredCleanPlateProvider` (真 pipeline 延后 stop-condition) + `FakeCleanPlateProvider` (dummy artifact id + warnings 明示"非真实产出"；SKIP method → OK + no artifact + warning；counter 递增给不同 request 产不同 dummy id；deep-copy 输入). `TextRedrawProvider` Protocol + `UnconfiguredTextRedrawProvider` + `FakeTextRedrawProvider` (按 `|translated|/|source|` 计算 expansion ratio；严格 > `max_expansion_ratio` → LAYOUT_OVERFLOW；缺 clean_plate → FAILED + CLEAN_PLATE_MISSING；缺 translated → FAILED). opus verifier 独立复审 20 条主张：**20/20 CONFIRMED**——23 adversarial 探针（BRAND_MARK 硬红线 / UNKNOWN 兜底 / UI license 强制 review / AUTHORIZED_INPAINT license_ref 合同层拦 / CleanPlate 引用一致 / layout/glossary/expansion 边界 / Fake counter / SKIP method / expansion 严格 >）全通过；121 条 test_invalid 全过（含 5 新负例）；34 条 domain + provider-sdk 测试全过；754 全套通过；lint 全绿；schema 零 drift；no REFUTED. → **VF-404 TTS Providers** (docs/modules/43 §7: 统一接口 `synthesize(text, language, voice_ref, style, target_duration, pronunciation_lexicon, seed) → AudioArtifact + WordTimings`): `contracts-py/tts.py` (3 new contracts `voice-profile` + `pronunciation-lexicon` + `tts-manifest`): `VoiceKind` enum (CLONED / PRESET / SYSTEM) + `VoiceLicenseStatus` (AUTHORIZED / PENDING / DENIED / UNCONFIRMED / EXPIRED) + `TTSProviderTier` (CLOUD_HIGH_QUALITY / SELF_HOSTED / SYSTEM_PREVIEW); `VoiceStyle` (pace/emotion/energy/pitch); `VoiceProfile` **CLONED 硬红线**——必须携带 `sample_source_ref` + `consent_ref`（合同层 model_validator 硬拦, §7）; `PronunciationLexicon` entries surface 唯一; `TTSWordTiming` + `TTSManifest` (sentence_id / voice_profile_id / text_hash / duration_ms / word_timings / speed_used / seed). 8 条 test_invalid 负例 + 3 v1 fixture + TS 生成 + schema 零漂移. `domain/tts.py` (纯函数): `text_hash_of` canonical sha256（41 §11 activity cache key 精神；text/lang/voice_id/style/target/lexicon/seed 全参与）; `validate_voice_profile` 5 issue: `LICENSE_NOT_AUTHORIZED` / `LICENSE_EXPIRED` (仅 now 给定时判定, 避免时钟依赖) / `CLONED_MISSING_SAMPLE` / `CLONED_MISSING_CONSENT` / `LANGUAGE_MISMATCH`. `is_authorized_for_synthesis` 便捷 gate. `suggest_speed(est, target) = est/target` **不 clip**——超区间由上层按 §8 顺序回退（1. LLM 改写 / 2. TTS 语速微调 / 3. Hold / 4. 伸缩 / 5. 重排）. `plan_tts_synthesis` 批量决策; **未授权 profile 直接跳过**（不 raise, 调用方能看漏了哪些）; speed 超自然区间 [0.92, 1.08] 加 `review_reasons`. `validate_tts_manifest` **7 issue**: AUDIO_MISSING / WORD_TIMINGS_EMPTY / WORD_TIMINGS_NOT_MONOTONIC / WORD_TIMING_OUT_OF_TOTAL / TEXT_HASH_MISMATCH / DURATION_OUT_OF_TOLERANCE (±10%) / SPEED_OUT_OF_NATURAL_BOUNDS. `provider-sdk/tts.py`: `TTSProvider` Protocol + `TTSStatus` (OK / FAILED / UNCONFIGURED / VOICE_UNAUTHORIZED / UNSUPPORTED_LANGUAGE) + `TTSErrorCode`. `UnconfiguredTTSProvider` 恒返 UNCONFIGURED（真云端 API + 权重延后 stop-condition）. `FakeTTSProvider`: **VOICE_UNAUTHORIZED 硬拦**（provider 层双保险——即使 domain 放行也拒, 防绕过路径）; zh 按字符 / en 按词 token; style.pace 参与时长; 每 word.confidence 固定 0.5（不假装高置信）; text_hash 与 domain.text_hash_of **逐位一致**（内联副本, 层级隔离——provider-sdk 不能 import domain）; deep-copy 输入. `TTSRouter` DEFAULT_ORDER (CLOUD → SELF_HOSTED → SYSTEM_PREVIEW): **VOICE_UNAUTHORIZED / UNSUPPORTED_LANGUAGE / OK 终局不 fallback**——品牌/安全一致性（不"降级"合成掩盖授权拒绝）; UNCONFIGURED / FAILED 才 fallback; 全 UNCONFIGURED → FAILED（不静默通过）. opus verifier 独立复审 **20 条主张 20/20 CONFIRMED**（无 REFUTED）——CLONED 合同硬拦 3 类反例（缺 sample/缺 consent/两者皆缺）+ 4 个非 AUTHORIZED 状态全被 validate + Fake 双拦 + text_hash provider/domain 逐位一致 + pace=2 时长减半 + word_timings 覆盖全区间且单调 + Router 优先级/回退语义 (UNCONFIGURED → fallback、VOICE_UNAUTHORIZED 终局) + 时长 ±10% 与语速 [0.92, 1.08] 边界严格. 826 全套通过 + lint 全绿 + schema 零 drift. → **VF-405 Duration Fit + Audio Mix** (docs/modules/43 §8 时长拟合 + §9 音频合成；合同层 `duration-fit-plan` + `audio-mix-plan` 前序已落地，本任务补 domain + provider-sdk): `domain/duration_fit.py`（纯函数）`decide_duration_fit` 实现 §8 **五步顺序回退** LLM_REWRITE→TTS_SPEED→BROLL_ADJUST→TIME_STRETCH→BEAT_REPLAN（命中即止）：语速落自然区间 [0.92,1.08] 直接 TTS_SPEED；改写候选估算落区间则 LLM_REWRITE 优先；否则把语速钳到自然边界，剩余时间差由相邻 B-roll slack 吸收（BROLL_ADJUST）或非人脸镜头小幅（≤±5%）时间伸缩（TIME_STRETCH，人脸锁定禁止）；都不够 → NEEDS_REVIEW + BEAT_REPLAN。**红线（禁止极端压速）**：任何 `OK_*` 决策的 `final_ratio`（最终语速倍数）恒落在自然区间内——超区间只得 NEEDS_REVIEW，绝不"OK 却用 0.7x 硬压"。`plan_duration_fit` 批量成 plan（自然区间/伸缩幅度写入 plan 供护栏复用同一阈值），`validate_duration_fit_plan` 四 issue: EMPTY_PLAN / **EXTREME_SPEED**（OK_* 的 final_ratio 超 plan 声明的自然区间——把红线变可检测）/ STATUS_METHOD_INCONSISTENT / REVIEW_WITHOUT_REASON. `domain/audio_mix.py` `build_audio_mix_plan`（人声在上、MUSIC/AMBIENCE 自动被 voice-activity Ducking、SFX 不压、total 取 max end）+ `validate_audio_mix_plan` 七 issue: EMPTY_MIX / NO_VOICE_TRACK / **DUCKING_CUT_POINT**（§9：切点触发会泵动，须 VOICE_ACTIVITY）/ DUCKING_WITHOUT_POLICY / DUCKED_BY_NON_VOICE（sidechain 触发源须人声轨）/ **VOICE_DUCKED**（只拦 VOICE_DUB 被压——§9 明确允许把 ORIGINAL_VOICE 压在 dub 下，替换配音降低原声）/ TRACK_OUT_OF_BOUNDS. `provider-sdk/duration_fit.py`（contracts-only，不 import domain）`DurationRewriteProvider` 端口 + Unconfigured + `FakeDurationRewriteProvider`（§8 步1 长度定向改写：**claims_unchanged 恒 True**——`_strip_filler_tokens` 只删被空白/标点边界分隔的**完整**犹豫音 token（中英统一、绝不子串捕获），LONGER 不凭空扩写，must_keep 保护；估算内联副本与 domain.estimate_duration_ms 逐位一致）。`provider-sdk/audio_mix.py` `AudioMixProvider` 端口 + Unconfigured + `FakeAudioMixProvider`（构造注入测量值、零 I/O、AST 断言零 ffmpeg/ebur128 导入；**True Peak Gate**：测得 true_peak 超 target → FAILED + TRUE_PEAK_EXCEEDED，与 QA VF-309 TRUE_PEAK_CLIP BLOCKER 对齐，合成层提前拦爆音）。8 条 test_invalid 负例 + domain/provider 新测试（含每轮 verifier 反例回归）。opus verifier **三轮**独立复审：round1 **REFUTED 两真缺陷**——**A**（est≪target 时 raw 舍入到 0 触发合同 `final_ratio>0` 校验崩溃，默认区间即可复现）、**B**（比容差更窄的自定义 natural_min 使 step-0 产出超区间 OK_UNCHANGED 而被自身护栏判 EXTREME_SPEED，build/validate 不自洽）→ 修（`_final_ratio` 正下限兜底；unchanged 容差带 `clamp` 进自然区间）；round2 **CONFIRMED A/B 已修**（200k+120k fuzz 零崩溃零越界）+ 揭示 Fake honesty 残留（zh 子串 `.replace` 把 '唔知道'=不知道 切成 '知道'=知道，Claim 反转却仍 claims_unchanged=True）→ 修（换**边界感知整词删除**，中英统一，只丢整块等于犹豫音的 token）；round3 **CONFIRMED 机制闭合** + 再揭示**集合成员**残留（'err' 英文动词 "to err is human"→"to is human"、'唔' 粤语否定 "唔好"→"好" 虽像语气词却是内容词）→ 修（从 `_FILLER` 剔除 err/唔，保留真犹豫音 erm）。每轮反例均加回归测试。941 全套通过 + lint 全绿 + schema 零 drift. → **VF-406 LipSync Provider**（docs/modules/43 §10：资格判断 + 回退阶梯 + 非阻塞）: 合同 `contracts-py/lipsync.py`（注册 `lipsync-plan`）: `LipSyncMode`（OFF/AUTO_ELIGIBLE/FORCE_REVIEW）+ `LipSyncMethod`（GPU_SYNTHESIS + §10.2 回退阶梯 ORIGINAL_TAKE/BROLL_COVER/FASTER_CUT/KEEP_UNSYNCED）+ `LipSyncIneligibleReason`（8 条 §10.1 资格：MODE_OFF/NO_PRIMARY_FACE/MULTIPLE_FACES/FACE_TOO_SMALL/OCCLUSION_HIGH/LOW_SPEAKER_PROBABILITY/DURATION_OUT_OF_RANGE/HEAD_TURN_TOO_LARGE/DUB_NOT_ALIGNED）+ `LipSyncReviewReason`；`LipSyncEligibilityCriteria`（脸数/面部高度比/遮挡/speaker 概率/时长上下界/转头/配音对齐阈值）、`LipSyncQAReport`（边界/肤色/运动/身份分 ∈[0,1] + passed）、`LipSyncSegmentDecision`（合同层硬拦：GPU_SYNTHESIS ⟹ eligible；eligible=False ⟹ 必有 ineligible_reasons；end≥start）、`LipSyncPlan`（segment_id 唯一 + 内嵌 criteria）。6 条 test_invalid 负例 + v1 fixture + TS + schema 零漂移。`domain/lipsync.py`（纯函数）: `assess_eligibility`（§10.1 资格门，收集**全部**不符原因，无单脸时跳过脸区几何检查避免误报，mode=OFF→MODE_OFF）、`resolve_fallback`（§10.2 严格阶梯，`KEEP_UNSYNCED` 终局永远可用）、`decide_lipsync`（合格→GPU 意图/FORCE_REVIEW 标复核；不合格→回退+警告；**合成成功 = QA 通过且有产物**，QA 未过或无产物一律**自动降级**）、`plan_lipsync`、`validate_lipsync_plan` 六护栏（EMPTY_PLAN / **QA_FAILED_NOT_DOWNGRADED**（红线：QA 未过却仍标 GPU）/ QA_PASS_WITHOUT_ARTIFACT / FORCE_REVIEW_NOT_FLAGGED / DOWNGRADE_WITHOUT_WARNING / OFF_MODE_NOT_KEEP_UNSYNCED）。**红线（§10.2 + §13 验收）：口型非阻塞**——每片段都落到具体非阻塞 method，合成失败/QA 不过**自动降级**并形成警告，绝不永久卡 Workflow。`provider-sdk/lipsync.py`（contracts-only，不 import domain）: `LipSyncProvider` 端口 + `UnconfiguredLipSyncProvider`（真 GPU MuseTalk 类 + 权重 + 许可待清 → stop-condition）+ `FakeLipSyncProvider`（**零 GPU/模型/subprocess 导入**，AST 断言；**不自行降级**——原样返回 OK + 注入 QA 交 domain 决定，fail=True→SYNTHESIS_FAILED；deep-copy 输入）。MuseTalk 注册 `third_party_manifest.yaml` L2（模型权重 + 许可待清红线）。opus verifier **两轮**独立复审：round1 8 主张 7 CONFIRMED（红线 Claim 1 在 373k-combo fuzz 下 0 违规）+ **REFUTED build/validate 自洽性**（`decide_lipsync` 把 synthesized_artifact_id 透传未与 qa.passed 耦合 → QA 过但无产物产出 GPU_SYNTHESIS 决策被自身 `QA_PASS_WITHOUT_ARTIFACT` 护栏判违）→ 修（合成成功须 QA 过**且**有产物，缺产物按不完整降级，review 记 SYNTHESIS_FAILED）+ 回归测试 + fuzz 解耦 qa/产物；round2 CONFIRMED（373k 解耦 fuzz 0 plan 违规 + 红线仍 holds + 无回归）。981 全套通过 + lint 全绿 + schema 零 drift. → **VF-407 Localization Review**（docs/modules/43 §12 语言 QA + §13 句级批准/局部重跑）: 合同 `contracts-py/localization_review.py`（注册 `localization-qa-report` + `localization-review`）: `LocalizationQACheck`（§12 检查项枚举：Claim/数字/专名/否定一致、术语/读音、未译 TextTrack、字幕行长/CPS/重叠/安全区、TTS 空白/重复/截断/音量/爆音、时长超限/极端变速、口型/音画同步）+ `ReviewState`（PENDING/APPROVED/REJECTED/EDITED）+ `ReRunStage`（TRANSLATION/TTS/SUBTITLE/LIPSYNC/AUDIO_MIX/RENDER）；`LocalizationQAFinding`（逐句发现，复用 VF-309 `QASeverity`）、`LocalizationQAReport`（**交叉校验硬拦：pass_or_block=True 不能与任何 BLOCKER 并存**——手工报告骗不过发布门）、`SentenceReviewDecision`（EDITED ⟺ 携 edited_text 合同层拦）、`LocalizationReview`（sentence_id 唯一）。4 条 test_invalid 负例 + 2 v1 fixture + TS + schema 零漂移。`domain/localization_review.py`（纯函数）: **§12 纯一致性检查** `check_number_consistency`（源/目标数字多重集、去千分位；不一致→**BLOCKER**，§13 数字一致率 100%）+ `check_negation_consistency`（否定标记数对齐，zh 不/没/无… + en not/no/n't…；不等→MAJOR 启发式）+ `run_consistency_checks`；`validate_localization_publish_gate`（BLOCKER→拦 / MAJOR 超阈→拦 / 否则放行）+ `aggregate_localization_qa`（装配逐句报告 + 算门，与合同交叉校验一致——2000 随机集 0 违规）；**`compute_rerun_scope`（§13 headline）**：改单句译文 → **只重跑该句** TTS/字幕/口型 + 全局 AUDIO_MIX/RENDER（一次）；未编辑句进 `unaffected_sentence_ids` 且**零阶段**，`TRANSLATION` 绝不进重跑集（改译文本身是输入）；`sentence_review_queue` + `validate_localization_review`（**APPROVED_OVER_BLOCKER**：不能批准仍带 BLOCKER 的句；EDITED/REJECTED 放行）。`provider-sdk/localization_review.py`（contracts-only，不 import domain）: `LocalizationConsistencyProvider` 端口（§12 语义级 Claim/专名一致需 LLM → stop-condition）+ Unconfigured + `FakeLocalizationConsistencyProvider`（浅层确定性专名保留检查——源专名未现于译文→MAJOR PROPER_NOUN，大小写不敏感；诚实标注不做深层语义；零重导入 AST 断言）。opus verifier **一轮 6/6 CONFIRMED**（compute_rerun_scope 全子集 fuzz 0 违规 + 数字一致多种篡改全 BLOCKER + 门/合同 2000 集一致 + 审核护栏 + provider 诚实 + 层级/lint/schema 全绿），无 REFUTED；一非阻断观察：数字检查按数字串多重集比对、符号盲（-5 vs 5 同集）——设计如此，深层语义由 consistency provider 兜。1022 全套通过 + lint 全绿 + schema 零 drift. → **P4 Exit 验收关口**（`tests/acceptance/`，报告 `docs/implementation/54d-p4-exit-report.md`）: 20 条中英 AI/科技样本（`p4_samples.py`，10 zh→en + 10 en→zh，AI/非-AI 各半，40 句忠实译对，刻意含数字/产品名/否定）跑 VF-401..407 domain + Fake。**① 数字/产品名/否定一致率 100%**（40 句零一致性发现 + 发布门放行；非空跑护栏：删数字必被 NUMBER_CONSISTENCY BLOCKER 抓 27 句、删专名必被 MAJOR 抓 7 句）。**② 字幕阅读速度 100% 过**（VF-402 pack_lines_into_cue → validate_subtitle_track 零违规；安全区由模板 SafeAreaSpec 结构性保证——诚实标注为结构而非断言检查；600ms 压测必触 READING_SPEED_EXCEEDED）。**⑤ 口型失败自动降级不阻塞**（40 句混合 10 通过/10 QA失败/10 不合格/10 意图 → plan 恒 validate 空、无 GPU 带失败 QA；全失败+无回退 → 全 KEEP_UNSYNCED + 警告）。**⑥ 改单句只重跑该句**（compute_rerun_scope 全 40 句变体：只该句 TTS/字幕/口型 + 全局一次、39 句零重跑、TRANSLATION 绝不重跑；分区性质任意子集成立）。诚实登记延后：**③ 高置信未翻译率 < 2%**（VF-403 护栏已在，真实 OCR 高置信轨样本待 stop-condition）、**④ 成片音画偏差 P95 ≤ 80ms**（结构不变量已保，真实毫秒度量待 media-core RenderRuntime + golden media）。opus verifier **6/6 CONFIRMED 无 REFUTED**（27/40 含数字、12/40 含否定、7/40 含专名——0 发现非空；tamper 护栏真抓；lipsync 混合真降级；rerun 分区真成立；报告不夸大，安全区结构保证已明示）。跑 `tests/acceptance/` → 55 passed；全套 1036 passed + lint 全绿 + schema 零 drift. **P4 本地化 CLOSED**（判定/护栏/作用域层面；真实引擎接入 + golden media 音画度量随后补齐）。→ **VF-501 Review Policy + Queue**（P5 审核与发布起步；docs/modules/44 §1 审核 + §2 严重级 + §12 Trusted Template）: 合同 `contracts-py/review_policy.py`（注册 `review-decision` + `review-policy` + `template-trust-state`）: `ReviewDecision`（§1.2：decision/scope/entity_id/entity_version/content_digest/reviewer/policy_snapshot/qc_reports/signature）+ `ReviewPolicyMode`（ALWAYS/NEW_TEMPLATE_ONLY/AUTO）+ `ReviewSeverity`（INFO/WARNING/ERROR/FATAL，区别于 VF-309 QA 尺度）+ `TemplateTrustLevel`（NEW/TRUSTED）+ `TemplateTrustState/Stats/Criteria`（§12 升级依据 + 阈值）。5 条 test_invalid 负例 + 3 v1 fixture + TS + schema 零漂移。`domain/review_policy.py`（纯函数）: `compute_approval_signature`（**无密钥内容绑定 sha256**）+ `is_approval_valid`（**修改失效**：APPROVED 且签名完整且 version + content_digest + 可选 entity_id 都匹配当前——任一改即失效，§1.2/§13）+ `publish_severity_gate`（§2：**FATAL→BLOCKED 不可覆盖**、ERROR→NEEDS_FIX、WARNING 看策略）+ `review_disposition`（严重级门优先覆盖策略，AUTO/TRUSTED 也降不了 FATAL/ERROR）+ `evaluate_trust_upgrade`/`promote_if_eligible`/`demote_on_change`（§12 NEW→TRUSTED 七条全达标才升；改关键字段 subtitle_style/beat/provider/publish_metadata → 回退 NEW）+ `validate_review_decision`（SIGNATURE_MISMATCH 篡改检测 + APPROVAL_STALE 陈旧审批）。**安全边界（docstring 明示）**：signature 是无密钥完整性绑定（保证审批绑定内容 + 篡改可检测 + 改即失效），**非密码学不可否认性**（HMAC/非对称 + 密钥管理属安全硬化项，接入真实签名走 security 评审）；真实平台发布凭据是 stop-condition。无 provider（纯策略/状态机）。opus verifier **安全向复审 5/5 CONFIRMED 无 bypass**：修改失效不可绕（改版本/内容/篡改字段/重签指向非当前内容全拦、JSON 分隔符注入不碰撞）、严重级门 192 组合零违规（FATAL 永远 BLOCKED、AUTO/TRUSTED 降不了阻塞级）、状态机每条准则独立拦升级 + 关键字段大小写变体都触发降级、契约负例全发射、domain 纯（无时钟/IO）；一 observation（is_approval_valid 实体绑定原在签名不在门参数）→ 已补 `current_entity_id` 门层纵深防御 + 回归。1075 全套通过 + lint 全绿 + schema 零 drift. → **VF-502 Platform Preflight**（docs/modules/44 §3 连接器能力 + 预检 + §9 元数据规则）: 合同 `contracts-py/publish_preflight.py`（注册 `platform-publish-spec` + `publish-connector-capability` + `preflight-report`；未注册输入 `PublishMediaProbe`/`PublishMetadata`）: `PublishPlatform`（TIKTOK/DOUYIN）+ `PublishMethod`（§3 阶梯 OFFICIAL_API/OFFICIAL_SHARE_SDK/BROWSER_AUTOMATION/ANDROID_DEVICE/MANUAL_EXPORT）+ `AuthStatus`/`ClientReviewStatus`/`AccountStatus`（授权/客户端审核/账号）+ `PreflightCheck`（16 检查项）；`PlatformPublishSpec`（平台规则集：分辨率/宽高比/编码/容器/文件/时长/标题标签长度/禁用字符）、`PublishConnectorCapability`（§3 连接器**上报**能力 + 授权/审核/账号状态）、`PreflightReport`（复用 VF-501 `ReviewSeverity`；**交叉校验硬拦：publishable=True 不能与 ERROR/FATAL 并存**）。5 条 test_invalid 负例 + 3 v1 fixture + TS + schema 零漂移。`domain/publish_preflight.py`（纯函数、无时钟）: `run_preflight`（媒体 7 检查 + 元数据 5 检查 + 授权/审核/账号：未授权/账号封禁→ERROR/FATAL 阻塞、未审核客户端→WARNING 可见性受限不阻塞 §3.1、连接器 max_file_size 收紧平台上限取 min）+ `publish_preflight_gate`（无 ERROR/FATAL 即 publishable）+ `select_publish_method`（§3 优先级阶梯选可用连接器，first-usable-wins，MANUAL_EXPORT 终局无需授权，都不行→None）+ `validate_preflight_report`（一致性护栏）。`provider-sdk/publish_connector.py`（contracts-only）: `PublishConnector` 端口（§3 只上报能力不由 UI 猜）+ `UnconfiguredPublishConnector`（诚实上报 available=False + UNAUTHORIZED，绝不假装能发）+ `FakePublishConnector`（回放注入能力、零网络、AST 断言不触真实平台）。opus verifier **5/5 CONFIRMED 无 REFUTED**：21 种阻塞条件独立触发全 publishable=False（无一漏放）、未授权/账号封禁绝不可发 + 未审核只 WARNING 不阻塞、方法阶梯全授权组合零错选（不选未授权非 manual）、契约/门一致、provider 诚实、domain 纯无时钟；一 observation（同方法多能力 first-wins 遮蔽）→ 已改 first-usable-wins + 回归。1114 全套通过 + lint 全绿 + schema 零 drift. **⚠️⚠️ Next roadmap task: VF-503 TikTok Official Publish 是 STOP-CONDITION**（docs/modules/44 §3.1/§4/§5：Content Posting API Direct Post、Creator Info 查询、上传/状态/Webhook、幂等）——**触及真实平台账号 + 官方 API + 应用审核 + 真实凭据**，`docs/implementation/53` §10 停止条件明确要求：**保持可用的 Fake/Manual adapter，接入真实平台前先向用户提出最小决策问题、不擅自扩范围**。VF-504 抖音官方发布 / VF-505 浏览器发布 adapter 同属 stop-condition。→ **VF-503 TikTok Official Publish（Fake-first，无 live network）**（docs/modules/44 §4 PublishWorkflow + §5 幂等与未知状态）: 合同 `contracts-py/publish_job.py`（注册 `publish-job`；复用 VF-502 `PublishPlatform`/`PublishMethod`）: `PublishState`（PENDING/PREFLIGHT_BLOCKED/AWAITING_AUTH/UPLOADING/SUBMITTED/VERIFYING/SUCCEEDED/SUCCEEDED_RECONCILED/FAILED/WAITING_FOR_HUMAN）+ `PublishAttempt`（每次调用存 request_digest + external upload/post token，token 是幂等对账依据）+ `PublishJob`（idempotency_key + 状态 + 外部 ID/URL/指纹 + attempts；合同硬拦：SUCCEEDED* 必带 external_post_id）。4 条 test_invalid 负例 + v1 fixture + TS + schema 零漂移。`domain/publish_job.py`（纯函数、无时钟/IO/网络）: `compute_idempotency_key`（§5：sha256(account+platform+render+metadata+window)，逐输入敏感）+ `PUBLISH_TRANSITIONS`（§4 合法迁移，**SUBMITTED 之后绝不回 UPLOADING/再 SUBMITTED**，终态无出边）+ `has_submitted`/`can_submit`（**只在 UPLOADING 且从未提交时允许提交**——external_post_token 是 durable latch）+ `record_submission`（**幂等硬拦**：已提交再提交抛 IllegalPublishTransition，绝不重复发布 §13）+ `reconcile_publish`（§5：查到已存在外部帖子 → SUCCEEDED_RECONCILED，未查到 → VERIFYING 继续查、**绝不盲目重发**）+ `confirm_success`/`to_waiting_for_human` + `validate_publish_job`（DOUBLE_SUBMIT/IDEMPOTENCY_KEY_MISMATCH/SUBMITTED_WITHOUT_ATTEMPT/SUCCEEDED_WITHOUT_EXTERNAL_ID）。`provider-sdk/publish_executor.py`（contracts-only）: `PublishExecutor` 端口（§3.1 creator_info/submit/query_status）+ `UnconfiguredTikTokPublishExecutor`（恒 UNCONFIGURED，绝不发布，零 live network）+ `FakeTikTokPublishExecutor`（确定性、零网络、**按 idempotency_key 幂等**——同 Job 重复 submit 返回同一 external_post_id + idempotent_replay=True，不产生第二帖；可注入 challenge/auth → CHALLENGE/AUTH_REQUIRED；AST/grep 断言无 requests/httpx/socket/subprocess）。opus verifier **5/5 CONFIRMED 无 REFUTED**：四路 double-publish 攻击全被拦（含 WAITING_FOR_HUMAN→UPLOADING 后仍因 token latch 拒重发）、对账不重发、幂等键逐输入敏感、provider 诚实 + AST/grep 确认零网络、契约负例全发射、domain 纯。一非阻断观察：double-publish 护栏依赖 `attempts` 的 external_post_token 不被清空——**接入持久化/workflow 层时须保证重建 Job 不剥离 attempts**（VF-503 纯域面之外，落地 PublishWorkflow 时注意）。**真实 TikTok 官方 API 集成（Direct Post/Creator Info/上传/Webhook 的 live 调用）仍是 stop-condition**——需真实账号 + 应用审核 + 凭据，接入前专项确认。1148 全套通过 + lint 全绿 + schema 零 drift. → **VF-504 Douyin Official Publish（Fake-first，无 live network）**（docs/modules/44 §3.2 抖音 OpenAPI + §4/§5）: 复用 VF-503 平台无关的 `PublishJob` 幂等状态机（`PublishPlatform.DOUYIN` 已在枚举）。新增 `provider-sdk/publish_errors.py` `map_publish_error(error_code, message)` → `PublishErrorMapping`（status + error_code + **human_required** + **retryable**）——§3.2 错误映射，工具中立、关键词驱动、**优先级从严**（授权 → 风控/挑战 → 限流 → 其它）：授权失效/风控挑战 `human_required=True`（转 WAITING_FOR_HUMAN 不盲目重试），限流 `retryable=True`，未知 FAILED+UNKNOWN。执行器 `publish_executor.py` **重构为 DRY 基类** `_UnconfiguredPublishExecutorBase`/`_FakePublishExecutorBase`（幂等核心 + `_post_url`/平台标签），`FakeTikTokPublishExecutor`（行为不变）与新增 `FakeDouyinPublishExecutor`/`UnconfiguredDouyinPublishExecutor` 均为薄子类；Fake 增 `rate_limited` 注入。抖音 Fake 幂等（同 idempotency_key 返回同一帖子）、query URL 为 douyin.com、challenge/auth/rate 注入映射到对应状态；Unconfigured 恒 UNCONFIGURED，零 live network（真实抖音 OpenAPI 完整错误码表在接入真实平台时补齐）。opus verifier **5/5 CONFIRMED 无 REFUTED**：错误映射优先级/安全语义（授权/风控绝不判 retryable——无不安全自动重试路径）、抖音 executor 诚实 + 幂等（两路 double-publish 攻击返回同一帖子）、**VF-503 域状态机 git diff 空（零回归）**、TikTok 8 测试全过、AST/grep 确认零网络零 domain 导入。一诚实 caveat：关键词覆盖有限（真实错误码表属 stop-condition，已在 docstring 标注）。1159 全套通过 + lint 全绿 + schema 零 drift. **真实平台 live 集成仍是 stop-condition**。→ **VF-505 Browser Publish Adapter（Fake-first，不驱动真实浏览器）**（docs/modules/44 §6）: 无新合同（复用 PublishJob/PublishExecutor）。`domain/browser_publish.py`（纯函数，§6 安全策略）: **`is_domain_allowed(url, whitelist)`**——**host 锚定白名单**（§6 只在白名单域名工作），`_extract_host` **与 WHATWG（浏览器/Playwright）一致**（剥离 tab/换行、反斜杠 `\` 按 `/`、authority 止于 `/?#`、去 userinfo/端口）+ **fail-closed LDH 主机名校验**（含 %/编码字节/空标签一律拒），挡 path 注入 / 后缀伪装 / `evil.com@`/`evil.com\.tiktok.com`/`evil.com#@tiktok.com`/`%2f`/`%00` 等解析差异绕过；`classify_browser_challenge` + `is_blocking_challenge`（登录失效/验证码/设备确认/内容警告/风控**一律阻塞→WAITING_FOR_HUMAN，绝不绕过** §6/§13）；`preferred_selector`（§6 role>label>test-id>css>coordinate，坐标最后回退）；`canary_ok`/`should_disable_connector`（页面结构契约失败→自动禁用 Connector）。`provider-sdk/publish_executor.py` 增 `UnconfiguredBrowserPublishExecutor` + `FakeBrowserPublishExecutor`（复用 DRY 基类；`allowed=False`→FAILED 拒发、`challenge_signal`→CHALLENGE 转人工无产物、否则幂等提交；零网络）。opus verifier **两轮**：round1 4/5（**REFUTED host 锚定**——`_extract_host` 未处理反斜杠，`evil.com\.tiktok.com` 通过白名单而 WHATWG 真实 host 是 evil.com；+ classifier 漏判空格分隔登录措辞）→ 修（WHATWG 一致解析 + fail-closed LDH + 拓宽 login 关键词）+ 5 回归；round2 CONFIRMED（44 差分向量对齐 Node `new URL`，0 可利用 parser-differential——%00/%2f WHATWG 直接抛错不可导航；挑战分类拓宽全过；无回归）。1188 全套通过 + lint 全绿 + schema 零 drift. **真实浏览器 live 驱动仍是 stop-condition**。→ **VF-506 Android Device Adapter（Fake-first，不驱动真实设备）**（docs/modules/44 §7 + §8 账号）: 合同 `contracts-py/publish_account.py`（注册 `platform-account`）: `PlatformAccount`（§8 账号——id/platform/display_name/external_account_id/**credential_ref 不透明 handle**/binding/connector_preferences/review_policy/publishing_window/locale/status/device_binding_id）+ `AccountBinding`（SERVER_ENCRYPTED/DESKTOP/DEVICE）；**红线：结构上无 token/password/cookie 字段**，`_no_secret_leak` 验证器**子串扫描**拒明文凭据特征（bearer/eyj JWT/password=/secret=/-----begin/refresh_token，含 handle 前缀包裹）。3+2 test_invalid 负例。`domain/android_device.py`（纯函数，§7.2 就绪门）: `check_device_readiness(state, expected_account_id, criteria)` → `DeviceReadiness`——电量/存储/网络/解锁/App 版本 + **红线 `WRONG_FOREGROUND_ACCOUNT`（前台账号 ≠ 目标 → 绝不发布，防发错号）**，收集全部不达标项；`can_dispatch_to_device`（§7.2 单设备同时只跑一个 Job）。`provider-sdk/publish_executor.py` 增 `UnconfiguredAndroidPublishExecutor` + `FakeAndroidPublishExecutor`（复用 DRY 基类；`device_ready=False`→FAILED 拒发、`device_confirm_required`→CHALLENGE 转人工无产物、否则幂等；零网络）。opus verifier **两轮**：round1 4/5——前台账号红线 72 组合 fuzz 0 违规 + executor 诚实 + 单设备单 Job + 无回归全 CONFIRMED，**REFUTED PlatformAccount 防误粘贴 guard**（前缀/空格分隔检查漏 `ch-eyJ...` 前缀包裹的 JWT，结构保证仍在）→ 修（子串扫描任意位置）+ 2 回归；round2 CONFIRMED（12 嵌入变体全拒、9 合法 handle 不误伤、无回归）。1209 全套通过 + lint 全绿 + schema 零 drift. **真实 Appium/ADB 驱动真机仍是 stop-condition**。→ **VF-507 Calendar + Reconciliation**（docs/modules/44 §4.4 WaitUntilSchedule + §5 幂等与未知状态）: 无新合同（复用 PublishJob）。`domain/publish_schedule.py`（纯函数）: `parse_publishing_window`（"HH:MM-HH:MM"，immediate/空→None）+ `is_within_publishing_window`（**支持跨零点窗口** 如 22:00-02:00）+ `next_publish_time`（Temporal Timer 目标——窗口前→今日开窗、窗口后→次日开窗、窗口内→立即；时刻由调用方传入无时钟）+ **`compute_copy_idempotency_key`**（§5 重复帖子防护：`copy_index=0` 即基础键→同内容去重绝不重复发布，`copy_index≥1` 才把 `#copy{n}` 折进窗口派生**不同**键→有意副本是新 Job）。`domain/publish_job.py` 增 `mark_manually_completed`（§5 手工完成：WAITING_FOR_HUMAN → SUCCEEDED_RECONCILED + 人工 external_id，**不新增提交、绝不再发**）+ **修 VF-503 潜在不一致**（PUBLISH_TRANSITIONS 补 WAITING_FOR_HUMAN → {SUCCEEDED, SUCCEEDED_RECONCILED}——此前 `reconcile_publish` 接受 WAITING_FOR_HUMAN 却被迁移表 assert 拒、会抛 IllegalPublishTransition）。opus verifier **5/5 CONFIRMED 无 REFUTED**：日程/跨零点/copy 键去重-vs-派生 + 手工完成只从 WAITING_FOR_HUMAN 且不新增 token + **关键：新增两条终态边不破坏无双发不变量**（终态携 external_id 不再提交、durable token latch 挡所有恢复回路的二次提交、post-submit 恒 can_submit=False、终态无出边）+ VF-503 18 测试全过零回归 + 纯函数无时钟。1223 全套通过 + lint 全绿 + schema 零 drift. → **P5 Exit 验收关口**（`tests/acceptance/test_p5_exit_*.py` + `p5_flow.py`，报告 `docs/implementation/54e-p5-exit-report.md`）: 用 VF-501..507 domain + Fake adapter 跑通发布闭环（**全程 Fake-first 不触真实平台**）。**① 端到端发布**（`run_publish` 串 VF-502 预检 publishable → 建 Job → submit → query_status → confirm_success → SUCCEEDED + external_post_id + 护栏空；TikTok/抖音/浏览器/Android 四 Fake 各跑通；非空跑：未授权/账号封禁/媒体不符 → 预检 publishable=False）。**② 网络超时重试不重复发**（超时 → reconcile 查到已存在帖子 → SUCCEEDED_RECONCILED 只一次 token；盲目二次 record_submission 硬拦——即使"等人工→恢复回 UPLOADING"回路仍被 durable token latch 拒；executor 按 idempotency_key 幂等）。**③ 挑战必停给人工**（浏览器登录/验证码/设备/内容警告/风控、真机确认/就绪门、官方 API challenge/auth → 非 OK 且 external_post_id=None 绝不绕过发布，Job 转 WAITING_FOR_HUMAN；浏览器非白名单域名 FAILED）。**④ 版本改动审批失效**（VF-501 改版本/内容/实体 → is_approval_valid 全失效）。**⑤ 前台账号确认**（VF-506 前台账号 ≠ 目标 → WRONG_FOREGROUND_ACCOUNT 不发错号）。opus verifier **5/5 CONFIRMED**（非空跑证明：idempotency 两测 message-match durable guard + latch 依赖真实、challenge 测真断 external_post_id=None、审批失效双向；一 minor 报告措辞已补媒体不符断言）。诚实登记 stop-condition：真实测试账号端到端 + 真实网络超时/风控 + §10 指标快照（P6）。跑 P5 exit → 17 passed；全 acceptance → 72 passed；全套 → 1240 passed + lint 全绿 + schema 零 drift. **P5 CLOSED.** → **VF-601 Metrics Connectors**（P6 效果反馈起步；docs/modules/44 §10 Performance Snapshot + 限流 + null 语义）: 合同 `contracts-py/performance.py`（注册 `performance-snapshot` + `snapshot-schedule` + `metrics-connector-capability`）: `PerformanceSnapshot`（§10 字段 observed_at/platform_post_id/age_hours/views/watch_time/avg_watch_time/completion_rate/likes/comments/shares/saves/follows/impressions/click_through_rate/source_confidence）——**红线（§10 null 语义）：每个指标字段 `… | None` 默认 None，绝不默认 0**（缺失=未知，`extra="forbid"` + 全 Optional 让"连接器没上报即 null"是结构保证而非约定）；非指标元数据（platform/post_id/account/observed_at/age/source_confidence）必填非空。`SnapshotSchedule`（1/3/6/24/72h/7d 快照计划；planned 非空/不重复、captured ⊆ planned）+ `MetricsConnectorCapability`（连接器上报 可用性/授权/`provided_fields`/`min_seconds_between_calls` 限流）+ `MetricField` 枚举。5 v1 fixture + TS + 10 条 test_invalid 负例 + schema 零漂移。`domain/performance.py`（纯函数）: 快照计划 `due_snapshot_ages`（到龄且未抓的 planned 年龄升序）/ `next_snapshot_age` / `next_snapshot_time`（Temporal Timer 目标）/ `record_capture`（不可变 + 幂等 + 拒非 planned 年龄）/ `is_schedule_complete` + `DEFAULT_SNAPSHOT_AGES_HOURS`；限流 `can_fetch_now`（边界含、last=None→可调）/ `next_allowed_fetch_time`；**null 语义** `relative_to_baseline`（§11 账号内相对指标原语，任一端 None 或基线 0 → None，绝不当 0）+ `validate_performance_snapshot` 六护栏**全 null-aware**——`ENGAGEMENT_EXCEEDS_VIEWS` / `VIEWS_EXCEED_IMPRESSIONS` 仅两端非空才比较，`COMPLETION_RATE_WITHOUT_VIEWS` 仅 views==0 触发（views=None 未知→跳过，这正是 null≠0），前三类（AGE_NEGATIVE/RATE_OUT_OF_RANGE/NEGATIVE_METRIC）为合同已拦的纵深防御。`provider-sdk/metrics_connector.py`（contracts-only，不 import domain）: `MetricsConnector` 端口 + `MetricsFetchResult`/`MetricsFetchStatus`（OK/RATE_LIMITED/UNCONFIGURED/AUTH_REQUIRED/NOT_FOUND/FAILED）+ `UnconfiguredMetricsConnector`（恒 UNCONFIGURED + snapshot=None，真实回采 stop-condition）+ `FakeMetricsConnector`（按 (post_id, age) 回放录制快照，未命中 NOT_FOUND；**保持 null 绝不编造 0**；入/出双深拷贝防污染；注入 rate_limited/auth_required/fail → 对应非 OK + snapshot=None；零网络，AST 断言无 requests/httpx/socket/subprocess）。opus verifier **5/5 CONFIRMED 无 REFUTED**（~40 adversarial 探针：null≠0 全路径、计划/限流边界含、Fake 双深拷贝 + 保持 null、AST 零网络零 domain 导入、层级/schema 全绿；两非阻断观察：due 用浮点精确匹配 program-fixed 年龄可接受、relative_to_baseline 未防极小非零基线属 §11 后续）。全套 1288 passed + lint 全绿 + schema 零 drift. → **VF-602 Performance Dashboard**（docs/modules/44 §11 归因）: 纯读模型计算（contracts + domain，无 provider——数据源是 VF-601 MetricsConnector；持久化 + Web UI 延后）。合同 `contracts-py/performance_dashboard.py`（注册 `video-performance-record` + `account-baseline` + `performance-dashboard`）: `VideoPerformanceRecord`（`PerformanceFeatures` 归因特征 template/hook/creation_mode/duration/language/publish_hour/weekday/qa_warning/manual_edit/trend_cluster + 抓到的 `PerformanceSnapshot`；快照 platform 必须与记录一致）、`AccountBaseline`/`AccountBaselineEntry`（账号 age×metric 基线 median/p25/p75/sample_count——账号内相对指标的分母）、`PerformanceDashboard`/`PerformanceGroupStat`（某 age+metric 视角各维度分组的相对表现 + `enough_samples`；**交叉校验：无账号基线中位数时分组不能有 median_relative**）、`GroupDimension` 枚举（TEMPLATE/HOOK/CREATION_MODE/DURATION_BUCKET/LANGUAGE/PUBLISH_DAYPART）。6 v1 fixture + TS + 7 条 test_invalid 负例 + schema 零漂移。`domain/performance_dashboard.py`（纯函数、确定性可复现）: `percentile`（线性插值 type-7）/ `metric_value_at_age`（同龄取最新观测）/ `relative_value` / 分桶（duration `[lo,hi)` / daypart / `bucket_for`，特征 None → 该维度跳过）/ `compute_account_baseline_entry` / `build_account_baseline` / `build_dashboard` / `rank_groups` / `insufficient_sample_groups`。**四红线**：① **账号内相对**——`build_dashboard` 先按 account_id+platform 过滤，绝不跨账号比较绝对播放（两账号绝对差 10x 但相对模式相同 → 同一相对看板）；② **样本不足不排序**——`rank_groups` 只排 `enough_samples`（sample_count ≥ min_samples）组，thin(n<min) 组只 `insufficient_sample_groups` 报不排（§11 "有足够样本后再排序"）；③ **null≠0**（承接 VF-601）——基线/相对值只用非空样本（[None,100,200] median=150 count=2，绝不当 0）；④ **可解释**——只有中位数/分位数/计数，确定性无黑盒模型。护栏 `validate_dashboard`（ENOUGH_SAMPLES_INCONSISTENT / MEDIAN_WITHOUT_SAMPLES / RELATIVE_WITHOUT_BASELINE 纵深）+ `validate_records_single_account`（CROSS_ACCOUNT_RECORD 跨账号污染）。opus verifier **5/5 CONFIRMED 无 REFUTED**（adversarial：null≠0 全路径、账号相对不跨账号泄漏、thin 组永不入排序、percentile/确定性、契约+护栏防谎报；三非阻断观察均为读模型固有边界 non-defect——RELATIVE_WITHOUT_BASELINE 因契约先拦而不可达属纵深、读模型无法重算全手构假看板属固有、count>0-median-None 反向不报但不影响排序置信）。全套 1322 passed + lint 全绿 + schema 零 drift. → **VF-603 Learning Signals**（docs/modules/44 §11 归因与学习信号）: 纯读模型（contracts + domain，无 provider）。合同 `contracts-py/learning_signal.py`（注册 `learning-signal-result` + `learning-report`）: `LearningSignalResult`（signal/metric/age + Spearman `correlation` ∈[-1,1] + `direction` + `buckets` + **`association_only` 恒 True** + note；model_validator：association_only 必须 True、样本不足时 direction 只能 INSUFFICIENT/NONE）、`LearningReport`（account/platform/age/metric/min_samples + signals）、`SignalKind`（QA_WARNING/MANUAL_EDIT/HUMAN_SELECTED/REJECTED_THEN_REVISED/TREND_HOTNESS/PUBLISH_DELAY/DURATION/HAS_MUSIC）+ `SignalDirection`（POSITIVE/NEGATIVE/NONE/INSUFFICIENT）。`PerformanceFeatures` 增 4 个**向后兼容** Optional 过程信号字段（human_selected/rejected_then_revised/trend_hotness_at_publish/publish_delay_hours；旧 v1 fixture 逐字节不变仍解析——verifier 显式验证）。2 v1 fixture + TS + 6 条 test_invalid 负例 + schema 零漂移。`domain/learning_signals.py`（纯函数、确定性）: `spearman_correlation`（标准秩相关含并列平均秩，∈[-1,1]，n<2/零方差→None——透明可复现无黑盒）/ `signal_value`（布尔→1.0/0.0，缺失→None）/ `analyze_signal` / `build_learning_report` / `significant_signals` / `_bucketize`（布尔 是/否，数值 低/中/高三分位）。**四红线**：① **关联非因果**——association_only 恒 True，direction/note/significant_signals 只报相关绝不作因果建议；② **账号内相对**——build_learning_report 先按 account+platform 过滤，结局用相对指标；③ **样本不足不下结论**——INSUFFICIENT，不给方向；④ **null≠0**——(信号,结局) 两端非空才成对，缺失排除。护栏 `validate_learning_report`（CAUSAL_CLAIM / CORRELATION_OUT_OF_RANGE / DIRECTION_WITHOUT_SAMPLES / ENOUGH_SAMPLES_INCONSISTENT / SIGNAL_LENS_MISMATCH）。opus verifier **5/5 CONFIRMED 无 REFUTED**（手算 Spearman 并列值 + 2000 例 Fraction 精确 fuzz 逐位一致；association_only 契约硬拦；样本不足 direction 门；null≠0 与账号隔离；PerformanceFeatures 向后兼容旧 fixture 仍解析；两非阻断观察均纵深冗余 non-defect）。全套 1348 passed + lint 全绿 + schema 零 drift. Next roadmap task: **VF-604 Ranker Calibration**（docs/modules/44 §11 末：样本足够后校准热点/Highlight 权重——**先离线评估再少量探索、保留探索流量**，不直接上黑盒排序；用 VF-601..603 的相对指标 + 学习信号做离线校准；非发布类 Fake-first 可做）——P6 收官任务。真实平台官方数据 API 回采属 stop-condition。Git repo with trunk `main`; one task = one branch + `--no-ff` merge. `docs/` remains the implementation spec.

## Common commands

```bash
uv sync && corepack pnpm install   # install deps (no global pnpm on this machine — use corepack pnpm)
make dev          # build + start full stack (docker compose)
make dev-infra    # infra containers only; then `make api` / `make web` for local hot-reload
make lint / make fmt / make test / make ci
make smoke        # health-probe running stack (API :8000, Web :5173, Temporal :8233)
make down         # stop containers (keeps volumes)
uv run pytest apps/api/tests/test_health.py::test_healthz_reports_ok   # single test
```

Local port overrides live in `.env` (gitignored): this machine moves Postgres/Redis/MinIO to 15432/16379/19000/19001 because another compose stack (`botinkit_dev`) owns the defaults. Temporal's `auto-setup` needs explicit `DB_PORT=5432` and binds to the container IP, not localhost (healthcheck probes `$(hostname)`).

All docs are Simplified Chinese; keep new or edited docs in Chinese. Docs follow a numeric naming convention (`00-` master plan, `10-`/`20-` overviews, `30s-` architecture, `40s-` modules, `50s-` implementation, `90-` references).

## Spec-kit workflow

Feature work runs through GitHub spec-kit v0.13.0 (`.specify/`) via Claude skills:

`/speckit-specify` → `/speckit-clarify` → `/speckit-plan` → `/speckit-tasks` → `/speckit-implement`

Support commands: `/speckit-analyze` (cross-artifact consistency), `/speckit-checklist`, `/speckit-converge`, `/speckit-constitution`, `/speckit-taskstoissues`.

- Active feature state lives in `.specify/feature.json` (works without git branches).
- Helper scripts in `.specify/scripts/bash/` (`create-new-feature.sh`, `check-prerequisites.sh --json`, `setup-plan.sh`, `setup-tasks.sh`) are invoked by the skills; rarely run manually.
- `.specify/memory/constitution.md` is still the **unfilled template** — run `/speckit-constitution` before treating it as authoritative.

## Documentation map (reading order)

1. `docs/00-master-plan.md` — scope, MVP boundaries, architecture principles, phase gates
2. `docs/10-module-overview.md` — modules M01–M15, data boundaries, Provider types, QA gate order
3. `docs/20-open-source-landscape.md` — which OSS projects to reuse and the license isolation levels (L0–L4)
4. `docs/architecture/30-system-architecture.md` — topology, Task Lease protocol, timeline layers, security
5. `docs/architecture/31-domain-model-and-workflows.md` — aggregates, state machines, events, schema evolution
6. `docs/modules/40–44` — per-module detailed designs
7. `docs/implementation/50-tech-stack.md` (ADRs), `51` (API contracts), `52` (repo layout), `53` (task roadmap), `54` (acceptance), `55` (macOS pilot)

## Planned architecture (binding decisions)

- **Topology**: Control plane in Docker Compose (FastAPI modular monolith `api` + Vue 3 `web` + Temporal + PostgreSQL/pgvector + Redis + MinIO) plus a Tauri 2 desktop app whose separate `edge-agent` sidecar survives UI exit, plus optional cloud GPU workers. Desktop workers make outbound-only connections via the Task Lease protocol (register / heartbeat / claim / renew / complete with idempotent submission).
- **ADRs** (`docs/implementation/50-tech-stack.md`): modular monolith, not microservices; Temporal is the source of truth for long-running task state (not Celery + status tables); `CreativeTimeline` is the timeline source of truth (OTIO for exchange; CapCut/剪映 drafts and Remotion trees are never system truth); desktop and cloud workers implement the same Task Protocol; official publish APIs first, browser automation second, Android real-device last; Remotion is conditionally adopted behind a `RenderProvider` due to its special license.
- **Stack pins**: Python 3.11 + FastAPI + Pydantic managed with `uv`; PNPM workspace, TypeScript strict, Vue 3 (React only inside the isolated Remotion package); Rust stable pinned by `rust-toolchain.toml`; SQLAlchemy 2 + Alembic; UUIDv7/ULID keys; UTC everywhere.
- **Planned monorepo layout**: see `docs/implementation/52-repo-structure.md` (`apps/`, `services/`, `packages/`, `connectors/`, `templates/`, `schemas/`, `tests/`, `fixtures/`, `infra/`). Docs name the root `videoforge/`; implement at this repo's root. Dependency direction: `apps → application → domain`; `domain` imports no frameworks (no FastAPI/SQLAlchemy/Temporal/FFmpeg); connectors implement application-defined ports; UI depends only on generated `contracts-ts`.
- **Test stack (planned)**: pytest + Hypothesis + testcontainers; Vitest + Playwright; cargo test; Temporal time-skipping tests; golden-media manifests comparing frames/audio/duration rather than file hashes; recorded fixtures for connectors — unit tests never hit live platforms.

## Implementation roadmap

`docs/implementation/53-coder-agent-roadmap.md` defines phases P0–P6 with task IDs `VF-001`…`VF-604`. Rules:

- Start at P0 (`VF-001` Bootstrap Monorepo) — never begin with UI or a platform crawler. VF-001's DoD establishes the dev command (`make`/`just dev` bringing up API, Web, Postgres, MinIO, Temporal).
- One task = one small PR using the template in `53` §9; tasks only start after their dependencies; each phase must pass its acceptance gate (`54-mvp-acceptance.md`) before the next.
- Wire real platforms/models through Fake Providers first, then swap implementations.

## Non-negotiable engineering rules (from docs/README.md §4)

- Every external platform, model, and asset source goes behind a Provider/Connector interface; business modules never depend on a third-party project's internals or temp directories — pass IDs and structured contracts only.
- Every media operation emits a replayable Job Manifest with input hashes, tool versions, and output hashes. Original facts (source video, raw ASR/OCR, source snapshots) are immutable; edits create new versions.
- Long tasks must be pausable, resumable, retryable, and idempotent — never in-process queues only.
- No plaintext platform credentials in UI, logs, or Task Envelopes; macOS uses Keychain, server uses an encrypted secret store with short-lived `credential_handle`s.
- On login failure, captcha, or risk-control challenges: transition to `WAITING_FOR_HUMAN`. Never attempt to bypass.
- Watermark/occlusion cleanup only for licensed material, with provenance records kept.
- GPL/AGPL code is never copied into the permissive-licensed core (isolation levels in `20-open-source-landscape.md` §3); new external tools are recorded in `third_party_manifest.yaml` first.
- Business modules never execute arbitrary shell strings; FFmpeg runs with argument arrays and path whitelists.
- macOS is the pilot, but macOS-specific logic lives only in platform adapters, never in domain/workflow code.

**Stop conditions** (`53` §10): needing real platform accounts, app review, or paid credentials; unclear licenses; captcha/risk challenges; changes to core ADRs, timeline truth, or the two creation-mode definitions; unexplained golden-sample regressions. Keep a working Fake/Manual adapter, and surface the blocker plus a minimal decision question instead of expanding scope.
